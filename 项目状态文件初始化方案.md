# 项目状态文件初始化方案

## 一、问题确认

三个状态文件都包含**错误数据**（来自另一本书"酒剑仙"的设定）：

| 文件 | 错误内容 |
|------|---------|
| `.character_state.json` | "扮演度提升"、"雷电操控"、"雷电法王" |
| `.chapter_extractions.json` | "扮演度达到90.0%"、"雷电异象" |
| `.world_state.json` | "扮演度提升"、"静电炸弹" |

## 二、初始化策略选择

### 方案对比

| 方案 | 方式 | 优点 | 缺点 | 推荐度 |
|------|------|------|------|--------|
| A | 规则解析（代码直接生成） | 快速、确定性强、无需AI调用 | 需要维护解析规则 | ⭐⭐⭐⭐⭐ |
| B | AI生成（调用API） | 灵活、可处理复杂逻辑 | 慢、有成本、可能出错 | ⭐⭐⭐ |
| C | 混合方案（规则+AI校验） | 兼顾确定性和灵活性 | 复杂度较高 | ⭐⭐⭐⭐ |

### 推荐：方案A（规则解析）

**原因：**
1. 初始化内容相对固定（初始状态都是确定的）
2. 不需要创造性，只需要正确映射
3. 速度快，用户体验好
4. 易于调试和维护

## 三、初始化规则设计

### 3.1 映射规则（从 phase_one_products 读取）

```python
# 映射表：一阶段产物 -> 状态文件
INITIALIZATION_RULES = {
    # 主角信息
    "protagonist_name": "角色设计.json -> protagonist.name",
    "protagonist_traits": "角色设计.json -> protagonist.traits",
    "protagonist_identity": "角色设计.json -> protagonist.identity",
    
    # 金手指/系统
    "system_name": "世界观设定.json -> power_system.shen_lang_exclusive (提取系统名称)",
    "system_mechanics": "世界观设定.json -> power_system.shen_lang_exclusive",
    "initial_power_level": "世界观设定.json -> power_system.level_standard (取最低等级)",
    
    # 初始能力
    "initial_abilities": "升级路线.json -> ability_system_progression.early_stage.mechanics",
    
    # 剧情线索
    "initial_plots": "阶段目标.json -> [0].key_deliverables",
    
    # 盟友
    "initial_allies": "角色设计.json -> core_allies",
    
    # 反派
    "initial_enemies": "角色设计.json -> main_antagonists.early_stage",
}
```

### 3.2 三个文件的初始化内容

#### 1. `.character_state.json`（角色状态）

```json
{
  "version": "2.0",
  "protagonist_name": "{从角色设计.json读取}",
  "protagonist": {
    "name": "{主角名}",
    "role": "主角",
    "health": "健康",
    "injuries": [],
    "abilities": [],
    "power_level": "{最低等级，如F级}",
    "location": "初始地点",
    "description": "{从角色设计.json读取}",
    "relationships": {},
    "introduced_chapter": 1,
    "last_appeared": 1,
    "changes_history": []
  },
  "allies": {
    "{盟友1名字}": {
      "name": "{盟友1名字}",
      "role": "{盟友1角色}",
      "health": "健康",
      "injuries": [],
      "abilities": [],
      "power_level": "未知",
      "location": "",
      "description": "{从角色设计.json读取}",
      "relationships": {},
      "introduced_chapter": null,
      "last_appeared": null
    }
  },
  "enemies": {
    "{早期反派名字}": {
      "name": "{早期反派名字}",
      "role": "反派",
      "health": "健康",
      "injuries": [],
      "abilities": [],
      "power_level": "未知",
      "location": "",
      "description": "{从角色设计.json读取}",
      "relationships": {},
      "introduced_chapter": null,
      "last_appeared": null
    }
  }
}
```

#### 2. `.world_state.json`（世界状态）

```json
{
  "protagonist": {
    "name": "{主角名}",
    "health": "健康",
    "injuries": [],
    "abilities_unlocked": [],
    "current_location": "初始地点",
    "relationships": {}
  },
  "allies": {},
  "enemies": {},
  "plot_threads": {
    "{阶段目标1}": {
      "name": "{阶段目标1名称}",
      "status": "active",
      "introduced_chapter": 1,
      "last_mentioned": 1,
      "priority": 10,
      "description": "{阶段目标描述}",
      "next_trigger": "{关键交付物1}"
    }
  },
  "system_rules": {
    "system_name": "{系统名称，如'弹幕干涉系统'}",
    "system_mechanics": "{系统机制简述}",
    "current_level": 0,
    "unlocked_skills": [],
    "special_states": []
  },
  "important_items": [],
  "global_events": []
}
```

#### 3. `.chapter_extractions.json`（章节提取）

```json
[]
```

**说明**：初始为空列表，每生成一章后追加提取的信息。

## 四、代码实现

### 4.1 创建 `ProjectStateInitializer` 类

```python
"""
项目状态初始化器
在生成第1章前，根据一阶段产物正确初始化三个状态文件
"""

import json
import logging
from pathlib import Path
from typing import Dict, List
from .phase_one_loader import PhaseOneDataLoader

logger = logging.getLogger(__name__)


class ProjectStateInitializer:
    """
    项目状态初始化器
    
    职责：
    1. 在项目启动时检查三个状态文件是否存在且正确
    2. 如果不存在或包含错误数据，根据 phase_one_products 重新初始化
    3. 确保状态文件与一阶段设定一致
    """
    
    def __init__(self, project_path: Path):
        self.project_path = Path(project_path)
        self.phase_one_loader = PhaseOneDataLoader(project_path)
        
        # 三个状态文件路径
        self.character_state_file = self.project_path / ".character_state.json"
        self.world_state_file = self.project_path / ".world_state.json"
        self.chapter_extractions_file = self.project_path / ".chapter_extractions.json"
    
    def initialize_if_needed(self, force: bool = False) -> bool:
        """
        如果需要，初始化项目状态文件
        
        Args:
            force: 是否强制重新初始化（即使文件已存在）
        
        Returns:
            bool: 是否执行了初始化
        """
        # 检查是否需要初始化
        needs_init = force or self._check_needs_initialization()
        
        if not needs_init:
            logger.info("[ProjectStateInitializer] 状态文件已存在且正确，跳过初始化")
            return False
        
        logger.info("[ProjectStateInitializer] 开始初始化项目状态文件...")
        
        # 加载一阶段数据
        phase_one_data = self.phase_one_loader.load_all()
        
        # 初始化三个文件
        self._init_character_state(phase_one_data)
        self._init_world_state(phase_one_data)
        self._init_chapter_extractions()
        
        logger.info("[ProjectStateInitializer] 项目状态文件初始化完成")
        return True
    
    def _check_needs_initialization(self) -> bool:
        """检查是否需要初始化"""
        # 检查文件是否存在
        files_exist = (
            self.character_state_file.exists() and
            self.world_state_file.exists() and
            self.chapter_extractions_file.exists()
        )
        
        if not files_exist:
            logger.info("[ProjectStateInitializer] 状态文件不存在，需要初始化")
            return True
        
        # 检查文件内容是否正确（简单检查：是否包含错误关键词）
        error_keywords = ["扮演度", "雷电操控", "雷电法王", "酒剑仙"]
        
        for file_path in [self.character_state_file, self.world_state_file]:
            try:
                with open(file_path, 'r', encoding='utf-8') as f:
                    content = f.read()
                    for keyword in error_keywords:
                        if keyword in content:
                            logger.warning(f"[ProjectStateInitializer] 发现错误关键词'{keyword}'，需要重新初始化")
                            return True
            except Exception as e:
                logger.error(f"[ProjectStateInitializer] 检查文件失败: {e}")
                return True
        
        return False
    
    def _init_character_state(self, phase_one_data: Dict):
        """初始化角色状态文件"""
        # 读取角色设计
        char_design = phase_one_data.get('character_design', {})
        protagonist = char_design.get('protagonist', {})
        core_allies = char_design.get('core_allies', [])
        early_antagonists = char_design.get('main_antagonists', {}).get('early_stage', [])
        
        protagonist_name = protagonist.get('name', '主角')
        
        # 构建主角信息
        protagonist_data = {
            "name": protagonist_name,
            "role": "主角",
            "health": "健康",
            "injuries": [],
            "abilities": [],
            "power_level": "F级（初始）",  # 从等级体系取最低
            "location": "现实世界/禁地入口",
            "description": protagonist.get('identity', '主角'),
            "relationships": {},
            "introduced_chapter": 1,
            "last_appeared": 1,
            "changes_history": []
        }
        
        # 构建盟友信息
        allies_data = {}
        for ally in core_allies:
            ally_name = ally.get('name', '盟友')
            allies_data[ally_name] = {
                "name": ally_name,
                "role": ally.get('role', '盟友'),
                "health": "健康",
                "injuries": [],
                "abilities": [],
                "power_level": "未知",
                "location": "",
                "description": ally.get('contribution', ''),
                "relationships": {},
                "introduced_chapter": None,
                "last_appeared": None
            }
        
        # 构建敌人信息
        enemies_data = {}
        for enemy in early_antagonists:
            enemy_name = enemy.get('name', '反派')
            enemies_data[enemy_name] = {
                "name": enemy_name,
                "role": "反派",
                "health": "健康",
                "injuries": [],
                "abilities": [],
                "power_level": "未知",
                "location": "",
                "description": enemy.get('motivation', ''),
                "relationships": {},
                "introduced_chapter": None,
                "last_appeared": None
            }
        
        # 构建完整数据
        character_state = {
            "version": "2.0",
            "protagonist_name": protagonist_name,
            "protagonist": protagonist_data,
            "allies": allies_data,
            "enemies": enemies_data
        }
        
        # 保存文件
        with open(self.character_state_file, 'w', encoding='utf-8') as f:
            json.dump(character_state, f, ensure_ascii=False, indent=2)
        
        logger.info(f"[ProjectStateInitializer] 角色状态文件初始化完成: {len(allies_data)}个盟友, {len(enemies_data)}个敌人")
    
    def _init_world_state(self, phase_one_data: Dict):
        """初始化世界状态文件"""
        # 读取数据
        char_design = phase_one_data.get('character_design', {})
        world_setting = phase_one_data.get('world_setting', {})
        stage_goals = phase_one_data.get('stage_goals', [])
        
        protagonist = char_design.get('protagonist', {})
        power_system = world_setting.get('power_system', {})
        
        protagonist_name = protagonist.get('name', '主角')
        
        # 提取系统名称（从shen_lang_exclusive字段）
        system_mechanics = power_system.get('shen_lang_exclusive', '')
        system_name = self._extract_system_name(system_mechanics)
        
        # 获取第一个阶段目标
        first_stage = stage_goals[0] if stage_goals else {}
        
        # 构建剧情线索
        plot_threads = {}
        if first_stage:
            goal_id = first_stage.get('goal_id', 'G1')
            plot_threads[goal_id] = {
                "name": goal_id,
                "status": "active",
                "introduced_chapter": 1,
                "last_mentioned": 1,
                "priority": 10,
                "description": first_stage.get('description', ''),
                "next_trigger": first_stage.get('key_deliverables', [''])[0] if first_stage.get('key_deliverables') else ''
            }
        
        # 构建世界状态
        world_state = {
            "protagonist": {
                "name": protagonist_name,
                "health": "健康",
                "injuries": [],
                "abilities_unlocked": [],
                "current_location": "现实世界/禁地入口",
                "relationships": {}
            },
            "allies": {},
            "enemies": {},
            "plot_threads": plot_threads,
            "system_rules": {
                "system_name": system_name,
                "system_mechanics": system_mechanics[:200] + "..." if len(system_mechanics) > 200 else system_mechanics,
                "current_level": 0,
                "unlocked_skills": [],
                "special_states": []
            },
            "important_items": [],
            "global_events": []
        }
        
        # 保存文件
        with open(self.world_state_file, 'w', encoding='utf-8') as f:
            json.dump(world_state, f, ensure_ascii=False, indent=2)
        
        logger.info(f"[ProjectStateInitializer] 世界状态文件初始化完成: 系统名称='{system_name}'")
    
    def _init_chapter_extractions(self):
        """初始化章节提取文件（空列表）"""
        with open(self.chapter_extractions_file, 'w', encoding='utf-8') as f:
            json.dump([], f, ensure_ascii=False, indent=2)
        
        logger.info("[ProjectStateInitializer] 章节提取文件初始化完成: 空列表")
    
    def _extract_system_name(self, mechanics_text: str) -> str:
        """从系统机制描述中提取系统名称"""
        if not mechanics_text:
            return "未知系统"
        
        # 尝试提取【】中的内容
        import re
        match = re.search(r'【(.+?)】', mechanics_text)
        if match:
            return match.group(1)
        
        # 尝试提取前20个字符
        return mechanics_text[:20] + "..." if len(mechanics_text) > 20 else mechanics_text


# 便捷函数
def initialize_project_state(project_path: Path, force: bool = False) -> bool:
    """便捷函数：初始化项目状态"""
    initializer = ProjectStateInitializer(project_path)
    return initializer.initialize_if_needed(force)
```

### 4.2 集成到章节生成流程

```python
# 在 ChapterConversationGenerator.__init__ 或生成第1章前调用

from .project_state_initializer import initialize_project_state

class ChapterConversationGenerator:
    def __init__(self, novel_data, project_path, ...):
        self.project_path = Path(project_path)
        
        # 初始化项目状态（如果是新项目或第1章）
        initialize_project_state(self.project_path)
        
        # 继续原有初始化...
```

## 五、使用方式

### 5.1 自动触发（推荐）

在 `ChapterConversationGenerator` 初始化时自动检查：

```python
def __init__(self, ...):
    # ... 其他初始化
    
    # 自动初始化项目状态
    from .project_state_initializer import initialize_project_state
    if self.project_path:
        initialize_project_state(self.project_path)
```

### 5.2 手动触发

在管理后台添加"重新初始化状态"按钮：

```python
# 管理接口
def reinitialize_project_state(project_id: str):
    project_path = get_project_path(project_id)
    initializer = ProjectStateInitializer(project_path)
    initialized = initializer.initialize_if_needed(force=True)
    return {"initialized": initialized, "message": "状态文件已重新初始化"}
```

## 六、验证方法

### 6.1 单元测试

```python
def test_project_state_initializer():
    """测试项目状态初始化器"""
    project_path = Path("test_project")
    
    # 创建初始化器
    initializer = ProjectStateInitializer(project_path)
    
    # 执行初始化
    result = initializer.initialize_if_needed(force=True)
    assert result == True
    
    # 验证文件内容
    with open(project_path / ".character_state.json") as f:
        char_state = json.load(f)
        assert "扮演度" not in str(char_state)  # 不应该有错误关键词
        assert "雷电操控" not in str(char_state)
    
    with open(project_path / ".world_state.json") as f:
        world_state = json.load(f)
        assert "弹幕干涉系统" in str(world_state) or "逻辑改写" in str(world_state)
```

## 七、实施步骤

1. **创建 `project_state_initializer.py`** - 实现初始化器类
2. **修改 `chapter_conversation_generator.py`** - 在初始化时调用
3. **修改 `world_state_manager.py`** - 使用正确的 `system_name` 而不是硬编码"扮演度"
4. **测试** - 验证新项目能正确初始化
5. **清理旧项目** - 为"开局带只二哈"项目重新初始化状态文件

---

## 八、紧急修复（立即可用）

如果立即需要修复"开局带只二哈"项目，可以手动执行：

```python
from web.services.market_driven.project_state_initializer import initialize_project_state
from pathlib import Path

project_path = Path("C:/work/xsdm/小说项目/yanghailin/开局带只二哈，我直播气哭邪神")
initialize_project_state(project_path, force=True)
```
