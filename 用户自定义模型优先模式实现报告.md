# 用户自定义模型优先模式 - 实现报告

## 功能概述
当用户配置了自己的API模型时，系统将**全程优先使用用户配置的模型**，并给用户明确的提示信息。

---

## 实现的核心逻辑

### 1. 自动检测与切换
```
用户配置自定义端点
    │
    ▼
系统启动时自动检测
    │
    ├─ 检测到自定义端点 ──▶ 启用"用户自定义模式"
    │                          - 强制使用 "custom" provider
    │                          - 显示醒目的提示信息
    │                          - 不自动切换到系统模型
    │
    └─ 未检测到自定义端点 ──▶ 使用系统默认模型
```

### 2. 用户提示机制

#### 后端日志提示（APIClient）
```
============================================================
🎯 用户自定义模型优先模式已启用
   检测到您配置了 N 个自定义API端点
   端点列表: [端点1名称], [端点2名称]
   平均折扣: XX% (节省 XX% 点数)
   💡 系统将只使用您的模型进行生成
   ⚠️  如需要使用系统模型，请删除或禁用您的自定义端点
============================================================
```

#### NovelGenerator 控制台提示
```
============================================================
🎯 用户自定义模型模式已启用
   检测到您配置了 N 个自定义API端点
   端点列表: [端点名称]
   💡 系统将只使用您的模型进行生成
   ⚠️  如需要切换到系统模型，请删除或禁用您的自定义端点
============================================================
```

#### 前端API状态（供前端展示）
```javascript
GET /api/custom-endpoints/status

响应:
{
  "success": true,
  "data": {
    "has_custom_endpoints": true,
    "endpoint_count": 1,
    "endpoint_names": ["my-deepseek"],
    "avg_discount_rate": 50,
    "message": "检测到您配置了自己的模型(my-deepseek)，系统将只使用您的模型进行生成",
    "note": "如需使用系统模型，请删除或禁用您的自定义端点"
  }
}
```

---

## 修改的文件清单

### 1. src/core/APIClient.py
**新增方法：**
- `_check_and_enable_user_custom_mode()` - 检测并启用用户自定义模式
- `is_user_custom_mode()` - 检查是否处于用户自定义模式
- `get_user_custom_info()` - 获取用户自定义端点信息

**修改方法：**
- `__init__()` - 在初始化后调用检测方法
- `call_api()` - 强制使用 custom provider
- `_get_provider_for_call()` - 用户模式下不自动切换

### 2. src/core/NovelGenerator.py
**新增方法：**
- `_check_and_display_user_custom_mode()` - 显示用户自定义模式提示

**修改方法：**
- `__init__()` - 初始化后调用检测提示方法

### 3. web/routes/custom_endpoint_routes.py
**新增API端点：**
- `GET /api/custom-endpoints/status` - 获取用户自定义端点状态

---

## 核心代码示例

### APIClient 用户自定义模式检测
```python
def _check_and_enable_user_custom_mode(self):
    """检查并启用用户自定义模型优先模式"""
    custom_pool = self.endpoint_pools.get("custom")
    if custom_pool and custom_pool.get_available_endpoints():
        custom_endpoints = custom_pool.get_available_endpoints()
        
        # 标记用户自定义模式
        self._user_custom_mode = True
        
        # 构建端点名称列表
        endpoint_names = [ep.name for ep in custom_endpoints]
        discount_rates = [getattr(ep, 'discount_rate', 50) for ep in custom_endpoints]
        avg_discount = sum(discount_rates) / len(discount_rates)
        
        # 显示醒目的提示信息
        self.logger.info("=" * 60)
        self.logger.info("🎯 用户自定义模型优先模式已启用")
        self.logger.info(f"   检测到您配置了 {len(custom_endpoints)} 个自定义API端点")
        self.logger.info(f"   端点列表: {', '.join(endpoint_names)}")
        self.logger.info(f"   平均折扣: {avg_discount:.0f}% (节省 {(100-avg_discount):.0f}% 点数)")
        self.logger.info("   💡 系统将只使用您的模型进行生成")
        self.logger.info("   ⚠️  如需要使用系统模型，请删除或禁用您的自定义端点")
        self.logger.info("=" * 60)
        
        # 将 custom 设为默认 provider
        if self.default_provider != "custom":
            self.default_provider = "custom"
```

### call_api 强制使用用户模型
```python
def call_api(self, system_prompt, user_prompt, ... provider=None, ...):
    """API调用 - 支持用户模型优先"""
    
    # 🔥 用户自定义模型优先模式
    if getattr(self, '_user_custom_mode', False):
        if provider and provider != "custom":
            user_str = self._get_username_str()
            self.logger.info(f"{user_str}🎯 用户自定义模式：强制使用 custom provider (忽略 {provider})")
        provider = "custom"
    
    # 原有逻辑...
```

### 用户模式下不自动切换
```python
def _get_provider_for_call(self, preferred_provider=None):
    """根据优先级和故障状态获取用于调用的 provider"""
    
    # 🔥 用户自定义模式：强制使用 custom provider，不自动切换
    if getattr(self, '_user_custom_mode', False):
        # 检查 custom 端点是否可用
        custom_pool = self.endpoint_pools.get("custom")
        if custom_pool and custom_pool.get_available_endpoints():
            return "custom"
        else:
            # 用户自定义端点不可用，给出明确的错误提示
            self.logger.error("=" * 60)
            self.logger.error("❌ 您配置的自定义模型当前不可用")
            self.logger.error("   系统不会自动切换到系统模型（用户自定义模式）")
            self.logger.error("   请检查您的API配置或删除自定义端点以使用系统模型")
            self.logger.error("=" * 60)
            return "custom"
    
    # 原有逻辑...
```

---

## 使用说明

### 对于用户
1. 配置自定义端点后，系统会自动检测并切换到用户自定义模式
2. 所有API调用将只使用用户配置的模型
3. 享受50%的点数折扣
4. 如需使用系统模型，需要删除或禁用自定义端点

### 对于开发者
1. 可以通过 `api_client.is_user_custom_mode()` 检查当前模式
2. 可以通过 `api_client.get_user_custom_info()` 获取用户端点信息
3. 可以通过 `/api/custom-endpoints/status` API 获取状态供前端展示

---

## 注意事项

1. **故障处理**：用户自定义模式下，如果用户端点失败，系统**不会**自动切换到系统模型，而是给出明确的错误提示
2. **多用户环境**：每个用户的自定义端点是全局共享的（通过 custom_endpoint_manager），需要在用户层面进行隔离考虑
3. **提示信息**：后端日志和前端展示都有相应的提示信息，确保用户了解当前使用的是自己的模型
