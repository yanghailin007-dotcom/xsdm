# 中期质量断层深度分析报告

## 一、问题定性：这不是正常现象

### 1.1 正常的长篇小说质量曲线

**理想的番茄爆款质量曲线**：
```
章节:  1-3   4-6   7-9   10-12  13-15  16-18  19-21  22-24
分数:   75    80    82     85     83     86     88     90+
趋势:   ↗     ↗     ↗      ↗      ↘↗     ↗      ↗      ↗
```

**黄金原则**：
- 前3章必须高位（75+），否则读者直接流失
- 中期（10-20章）稳步上升，维持在80+
- 每3-5章一个小高潮，质量波动不超过±5分

### 1.2 本书实际质量曲线（异常）

```
章节:  1-3   4-6   7-9   10-12  13-15  16-18  19-21  22-24
分数:   83    85    71     57     67     73     93     96
趋势:   ↗     ↗     ↘      ↘      ↗      ↗      ↗      ↗

异常区域: ████████████ 第8-12章（断层区）
```

**断层定义**：连续5章质量低于60分，与前后章节差距超过20分。

**结论**：这是典型的**中期塌陷**，不是正常现象。

---

## 二、根本原因：战术规划器的设计缺陷

### 2.1 TacticalPlanner的5章循环机制

```python
# _get_cycle_template 核心逻辑
cycle_pos = i % 5  # 0-4循环

cycles = [
    {"position": 1, "emotion": "压抑", "intensity": "7-8"},  # pos=0
    {"position": 2, "emotion": "紧张", "intensity": "8-9"},  # pos=1
    {"position": 3, "emotion": "反转", "intensity": "8-9"},  # pos=2
    {"position": 4, "emotion": "震惊", "intensity": "7-8"},  # pos=3
    {"position": 5, "emotion": "期待", "intensity": "6-7"},  # pos=4
]
```

### 2.2 第8-12章的循环位置分析

| 章节 | cycle_pos | 规划情绪 | 实际情绪 | 偏差 |
|------|-----------|---------|---------|------|
| 8 | (8-1)%5=2 | 反转 | 压抑(头痛) | ❌ 不匹配 |
| 9 | (9-1)%5=3 | 震惊 | 压抑(阴谋) | ❌ 不匹配 |
| 10 | (10-1)%5=4 | 期待 | 小爽(突破) | ⚠️ 部分匹配 |
| 11 | (11-1)%5=0 | 压抑 | 压抑(被困) | ✅ 匹配 |
| 12 | (12-1)%5=1 | 紧张 | 疲惫(车轮战) | ⚠️ 部分匹配 |

**问题发现**：
1. 第8-9章的规划情绪（反转、震惊）与实际生成（压抑）严重不符
2. 第11章虽然匹配"压抑"，但强度过高且持续时间过长

### 2.3 关键设计缺陷

**缺陷1：连续压抑章节过多**
```
第8章: 压抑 (头痛发作)
第9章: 压抑 (约翰阴谋)
第11章: 压抑 (被困毒瘴)
```
**3章压抑连续出现**，违反了"压抑后必须快速释放"的原则。

**缺陷2：反派视角占比失控**
```
第9章内容分布:
- 约翰视角: 40%
- 鹰酱会议: 30%
- 楚辰视角: 30%

问题: 反派视角占比70%，主角被动挨打
```

**缺陷3：爽点与压抑未交替**
```
标准节奏: 压抑→反转→爽点→期待
实际节奏: 压抑→压抑→小爽→压抑→疲惫
```

---

## 三、系统性问题诊断

### 3.1 战术规划器的3个致命错误

#### 错误1：模板化情绪循环不适用于中期

**5章循环模板的问题**：
```
[压抑]→[紧张]→[反转]→[震惊]→[期待]
   ↓
在中期（第8-12章），读者已经熟悉了主角实力
继续使用"压抑"开局会导致疲劳感
```

**正确做法**：
- 前6章：可以使用压抑（建立困境）
- 第7章后：应该以"小爽"或"震惊"开局，维持高位

#### 错误2：缺乏质量监控反馈机制

**BatchSummarizer的问题**：
```json
// batch_summary_007_012.json
{
  "chapter_number": 8,
  "quality_score": 8.0  // ← 虚假评分！
}
```

**实际质量**：
- 第8章番茄得分：71.2分
- 第9章番茄得分：59.4分
- 第11章番茄得分：50.9分

**问题**：Summarizer没有接入真实的质量分析数据，导致：
1. 系统无法检测到低质量章节
2. 无法触发自动修复或告警
3. 下一batch的规划基于错误数据

#### 错误3：阶段目标与章节规划脱节

```
阶段目标G1: "建立形象，解锁酒神咒"
          ↓
实际规划: 
  - 第8章: 头痛发作（与目标无关）
  - 第9章: 约翰阴谋（与目标无关）
  - 第11章: 被困毒瘴（偏离目标）
```

**正确做法**：每章必须明确标注对阶段目标的贡献度。

---

### 3.2 HierarchicalPlanner的调度问题

**Batch窗口设置**：
```python
batch_size = 6  # 每批6章
tactical_window = 30  # 战术规划窗口30章
```

**问题**：
1. Batch太小（6章），导致跨batch的情绪连贯性难以保证
2. 第7-12章在同一个batch，但这正好是一个"压抑-反转-震惊"周期
3. 如果batch边界落在反转点，会导致反转章节被切断

**建议**：
```python
batch_size = 3  # 改为3章，对应一个完整情绪小循环
# 或
batch_size = 5  # 对应一个完整情绪大循环
```

---

## 四、修复方案

### 方案A：立即修复（治标）

#### 步骤1：重写第8-12章

**第8章重写策略**：
```
原标题: 楚辰头痛发作，施展'斩龙'
新标题: 御剑术显威！全球直播间的疯狂

结构调整:
1. 删除"头痛发作"的压抑设定
2. 改为: 楚辰主动展示御剑术（爽点）
3. 增加: 全球直播间弹幕爆炸（对话）
4. 增加: 各国专家震惊反应（权威层）

预期效果: 对话比例21%→50%，得分71→85+
```

**第9章重写策略**：
```
原标题: 约翰联手鹰酱，准备报复
新标题: 鹰酱阴谋败露，楚辰隔空震慑

结构调整:
1. 减少约翰视角（40%→10%）
2. 增加: 楚辰通过直播察觉阴谋（主角主动）
3. 增加: 隔空一剑震慑约翰（爽点前置）
4. 删除: 冗长的会议讨论

预期效果: 对话比例21%→45%，得分59→80+
```

#### 步骤2：修复BatchSummarizer

```python
def summarize_batch(self, chapters, ...):
    # 接入真实质量数据
    for ch in chapters:
        metrics = analyze_chapter_quality(ch['content'])
        ch['quality_score'] = metrics['tomato_score']  # 不再是虚假的8.0
        
        # 检测低质量章节并告警
        if metrics['tomato_score'] < 60:
            logger.warning(f"第{ch['chapter_number']}章质量过低，建议重写")
```

---

### 方案B：系统重构（治本）

#### 重构1：情绪规划引擎升级

**新的情绪规划算法**：
```python
def plan_emotion_curve_v2(chapter_num, protagonist_power_level):
    """
    基于主角实力等级的动态情绪规划
    """
    if protagonist_power_level < 10:  # 前期
        base_emotions = ['压抑', '紧张', '反转', '震惊', '期待']
    elif protagonist_power_level < 30:  # 中期
        base_emotions = ['小爽', '震惊', '大爽', '期待', '反转']
    else:  # 后期
        base_emotions = ['震惊', '大爽', '超爽', '期待', '反转']
    
    # 确保没有连续压抑
    for i in range(len(base_emotions)-1):
        if base_emotions[i] == '压抑' and base_emotions[i+1] == '压抑':
            base_emotions[i+1] = '反转'  # 强制插入反转
    
    return base_emotions
```

#### 重构2：质量监控实时化

```python
class QualityMonitor:
    """实时监控章节质量"""
    
    THRESHOLDS = {
        'dialogue_ratio': 40,  # 对话比例<40%告警
        'tomato_score': 60,    # 得分<60告警
        'consecutive_depressing': 2  # 连续2章压抑告警
    }
    
    def check_chapter(self, chapter):
        alerts = []
        
        if chapter['dialogue_ratio'] < self.THRESHOLDS['dialogue_ratio']:
            alerts.append({
                'type': 'low_dialogue',
                'severity': 'warning',
                'message': f"第{chapter['num']}章对话比例过低"
            })
        
        if chapter['emotion'] == '压抑':
            self.depressing_count += 1
            if self.depressing_count >= self.THRESHOLDS['consecutive_depressing']:
                alerts.append({
                    'type': 'consecutive_depressing',
                    'severity': 'critical',
                    'message': f"连续{self.depressing_count}章压抑，必须插入爽点"
                })
        else:
            self.depressing_count = 0
        
        return alerts
```

#### 重构3：战术规划动态调整

```python
def adjust_tactical_plan(self, current_plan, quality_feedback):
    """
    根据质量反馈动态调整战术规划
    """
    # 如果上一batch质量低，调整下一batch
    if quality_feedback['avg_score'] < 60:
        # 强制插入爽点章节
        for ch in current_plan['chapters']:
            if ch['emotion'] == '压抑':
                ch['emotion'] = '反转'
                ch['event'] = '主角爆发反杀'
                ch['must_include'] = ['弹幕爆炸', '全球震惊']
    
    return current_plan
```

---

## 五、一句话总结

> **中期断层不是正常现象，而是TacticalPlanner的5章循环模板与中期剧情不匹配导致的系统性问题。根本原因是：1）连续3章"压抑"情绪违反了爽文节奏；2）BatchSummarizer使用虚假质量评分（8.0），导致系统无法检测和修复低质量章节；3）反派视角占比过高（70%），挤压了主角戏份和直播弹幕。**

---

## 六、修复优先级

| 优先级 | 措施 | 影响 | 工作量 |
|--------|------|------|--------|
| **P0** | 重写第8-12章 | 挽救读者留存 | 2-3天 |
| **P1** | 修复Summarizer质量评估 | 防止再次发生 | 半天 |
| **P2** | 升级情绪规划引擎 | 长期质量保障 | 1周 |
| **P3** | 实时质量监控 | 自动告警修复 | 3天 |

---

*分析完成时间：2026-04-02*
