# 章节生成策略分析报告

## 问题一：每章提取 vs 批次总结提取

### 当前实现

**每章生成后立即提取** (`ChapterInfoExtractor.extract_from_chapter`):
- 在每章生成完成后，立即调用 AI 提取结构化信息
- 提取内容：新角色、角色变化、新钩子、关键事件
- 保存到 `chapter_data["extracted_info"]`

**批次总结** (`BatchSummarizer.summarize_batch`):
- 每 6 章生成完成后，调用 AI 进行批次总结
- 基于已提取的章节信息进行汇总

### 对比分析

| 维度 | 每章提取 | 批次总结提取 |
|------|---------|------------|
| **API 调用次数** | 6 次（每批） | 1 次（每批） |
| **Token 消耗** | 高（6倍） | 低（1倍） |
| **提取精度** | 高（单章聚焦） | 中（批次局部） |
| **角色一致性** | 优（逐章追踪） | 中（局部汇总） |
| **实时性** | 高（立即可用） | 低（批次结束后） |
| **失败影响** | 局部（单章） | 全局（批次） |

### 我的建议：保持**混合模式**（当前实现是合理的）

**原因：**

1. **每章提取的价值**：
   - 单章提取可以捕获细节（如某个角色的微表情、小钩子）
   - 下一章生成时可以立即使用这些信息（如回收钩子）
   - 批次总结主要是**汇总**不是**替代**单章提取

2. **批次总结的价值**：
   - 6 章后需要一个高层次的回顾
   - 确保情节连贯性和角色发展弧线
   - 发现单章提取可能遗漏的长线索

3. **成本考虑**：
   - 单章提取仅分析前 2000 字（控制 token）
   - 模型可以是轻量级（如 kimi-k2的轻量版本）
   - 相比生成章节本身的成本，提取成本较低

### 优化建议

```python
# 可配置的提取策略
EXTRACTION_STRATEGY = {
    "mode": "mixed",  # 混合模式
    "single_chapter": {
        "enabled": True,
        "model": "kimi-k2-lite",  # 使用轻量级模型降低成本
        "max_content": 2000,
        "focus": ["角色", "钩子"]  # 只关注关键信息
    },
    "batch_summary": {
        "enabled": True,
        "frequency": 6,  # 每6章
        "model": "kimi-k2-pro",
        "focus": ["情节连贯", "角色弧光", "长线索回顾"]
    }
}
```

---

## 问题二：批次第一章是否需要设定输入轮次

### 当前流程

1. 用户创建项目时输入基础设定（标题、角色、金手指等）
2. 系统生成战术规划（每章大纲）
3. 批次生成时，直接按战术规划生成章节
4. **缺失：**批次第一章没有"设定输入"环节

### 问题分析

**为什么需要设定输入轮次？**

1. **AI 记忆衰减**：
   - 即使有 world_state.json，AI 对长文本设定的记忆仍然有限
   - 每批 6 章生成间隔，AI 可能"遗忘"关键设定

2. **角色一致性**：
   - 第一章的角色言行风格会影响后续章节
   - 需要在开始前强调"这个角色是什么样的人"

3. **设定演化说明**：
   - 每个阶段（6章）可能有不同的重点
   - 如第1-6章：建立人设；第7-12章：展开剧情

### 建议方案

**方案 A：自动化设定输入（推荐）**

在每批第一章生成前，自动构建一个"设定回顾"prompt：

```python
def build_batch_setup_prompt(self, batch_start: int, world_state: Dict, blueprint: Dict) -> str:
    """构建批次设定输入 prompt
    
    Args:
        batch_start: 批次起始章节
        world_state: 当前世界状态
        blueprint: 战术规划
    """
    return f"""
## 批次设定回顾（第{batch_start}-{batch_start+5}章）

### 核心设定（必须严格遵守）
**主角**: {world_state.get('protagonist', {}).get('name', '未命名')}
- 性格: {world_state.get('protagonist', {}).get('personality', '')}
- 能力: {world_state.get('protagonist', {}).get('abilities', [])}
- 当前状态: {world_state.get('protagonist', {}).get('current_state', '')}

**关键角色**:
{self._format_key_characters(world_state.get('allies', {}))}

**未解决钩子**:
{self._format_pending_hooks(world_state.get('pending_hooks', []))}

**本批重点**:
{blueprint.get(f'chapter_{batch_start}', {}).get('purpose', '')}

### 写作要求
1. 严格依据上述设定，不得偏离
2. 第一章要建立清晰的角色印象
3. 确保与上一批的连贯性

请开始生成第{batch_start}章...
"""
```

**方案 B：用户确认模式**

如果用户希望更高的控制度，可以在每批开始前增加确认步骤：

```python
# 在生成界面增加
if batch_start > 1 and batch_start % 24 == 1:  # 每4批（24章）
    show_stage_confirm_dialog(
        current_state=world_state,
        next_batch=f"第{batch_start}-{batch_start+5}章",
        suggestions=ai_generated_suggestions
    )
```

### 实施建议

**阶段性实施**：

1. **第一阶段**（立即）：实现方案 A - 自动化设定输入
   - 修改 `chapter_conversation_generator.py`
   - 在生成第一章前插入设定回顾 prompt
   - 利用已有的 world_state 和 batch_summary

2. **第二阶段**（后续）：根据用户反馈决定是否需要方案 B
   - 如果自动化设定输入后角色一致性仍有问题
   - 再考虑增加用户确认步骤

### 待修复代码位置

**修改文件**: `web/services/market_driven/chapter_conversation_generator.py`

**关键方法**: `_generate_single_chapter`

**修改点**: 在第一章（或每批第一章）生成前，添加设定回顾逻辑

---

## 总结

### 问题一结论

**保持现状**：每章提取 + 批次总结的混合模式是合理的
- 每章提取确保实时性和细节完整性
- 批次总结确保高层次一致性
- 可优化提取模型降低成本

### 问题二结论

**建议实施**：批次第一章增加自动化设定输入
- 利用已有 world_state 和 batch_summary
- 在生成前自动构建设定回顾 prompt
- 不增加用户操作负担
- 显著提升角色一致性和连贯性
