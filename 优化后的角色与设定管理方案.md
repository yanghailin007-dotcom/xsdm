# 优化后的角色与设定管理方案

## 核心变化

### 变化1: 角色管理前置到战术规划

**旧流程**:
```
战术规划（30章） → 章节生成（随意创造角色）
```

**新流程**:
```
战术规划（30章情节 + 30章角色） → 章节生成（只能用已规划角色）
```

### 变化2: 设定回顾放在批次开始（一次性）

**旧流程**:
```
每章都加设定提醒 → token浪费、重复
```

**新流程**:
```
批次第1章前: 基础设定 + 前情回顾（一次）
后续章节: 按原有提示词执行（使用已确定的设定和角色）
```

---

## 详细方案

### 第一部分: 战术规划改造（核心）

**修改文件**: `web/services/market_driven/tactical_planner.py`

**改造内容**:

#### 1.1 新增角色规划功能

```python
class TacticalPlanner:
    # 现有功能...
    
    def generate_tactical_plan_with_characters(
        self, 
        start_chapter: int,
        end_chapter: int,
        novel_title: str,
        protagonist_name: str,
        stage_goal: Dict,
        previous_summary: Dict = None,
        novel_data: Dict = None,  # 新增: 获取设定信息
    ) -> Dict:
        """
        生成带角色规划的战术规划
        
        Returns:
            {
                "chapters": [...],  # 现有：章节规划
                "characters": {     # 新增：角色规划
                    "core": [...],      # 核心角色（必须出场）
                    "major": [...],     # 重要配角（根据场景出场）
                    "minor": [...],     # 次要配角（可选）
                    "temporary": [...]  # 临时角色（随用随走）
                }
            }
        """
        # 1. 生成现有的情节规划
        tactical_plan = self.generate_tactical_plan(
            start_chapter, end_chapter,
            novel_title, protagonist_name,
            stage_goal, previous_summary
        )
        
        # 2. 新增：角色规划
        character_plan = self._plan_characters_for_batch(
            start_chapter=start_chapter,
            end_chapter=end_chapter,
            chapters=tactical_plan.get('chapters', []),
            novel_data=novel_data,
            previous_summary=previous_summary
        )
        
        tactical_plan['characters'] = character_plan
        return tactical_plan
```

#### 1.2 角色规划详细设计

```python
def _plan_characters_for_batch(
    self,
    start_chapter: int,
    end_chapter: int,
    chapters: List[Dict],
    novel_data: Dict,
    previous_summary: Dict
) -> Dict:
    """
    为本批次规划30章的角色
    """
    
    # 获取已有角色
    existing_chars = self._get_existing_characters(previous_summary)
    
    # 获取设定
    final_plan = novel_data.get('final_plan', {})
    user_choices = novel_data.get('user_choices', {})
    
    # 构建提示词让AI规划角色
    prompt = f"""你是一位小说角色设计专家。请为下面的30章规划角色。

## 核心设定（必须遵守）
**主角**: {user_choices.get('protagonist_name', final_plan.get('protagonist_name', '主角'))}
- 性格: {final_plan.get('protagonist_personality', '')}
- 能力: {final_plan.get('golden_finger_summary', '')}

**已有配角**: {', '.join(existing_chars)}

## 本批次情节大纲
{self._format_chapter_outline(chapters)}

## 角色规划要求

1. **核心角色** (必须出场):
   - 主角: 每章必须出场
   - 标配: 主角固定搭档（如二哈、系统等）

2. **重要配角** (根据场景出场):
   - 从已有配角中选择
   - 每章最多2个重要配角
   - 确保配角不被遗忘

3. **可选配角** (次要角色):
   - 可以是新角色，但需要说明作用
   - 必须有后续安排

4. **临时角色** (路人/小兵):
   - 随用随走，不需要名字

## 输出格式
```json
{{
    "core": [
        {{"name": "主角名", "type": "protagonist", "appearance": "每章"}},
        {{"name": "二哈", "type": "sidekick", "appearance": "90%章节"}}
    ],
    "major": [
        {{"name": "战友A", "type": "ally", "first_chapter": 32, "purpose": "帮助主角战斗"}},
        {{"name": "反派B", "type": "villain", "first_chapter": 35, "purpose": "本批次主要对手"}}
    ],
    "minor": [
        {{"name": "NPC甲", "type": "neutral", "first_chapter": 33, "purpose": "提供情报"}}
    ],
    "temporary": "可以有路人、小兵等无名角色"
}}
```

**禁忌**:
- 严禁创造与核心设定矛盾的角色
- 严禁让已下线角色突然出场
- 每章有名角色不超过4个
"""

    # 调用AI生成角色规划
    response = self.api_client.generate_content_with_retry(
        content_type="character_planning",
        user_prompt=prompt,
        temperature=0.7
    )
    
    # 解析角色规划
    character_plan = self._parse_character_plan(response)
    
    return character_plan
```

#### 1.3 章节与角色关联

```python
def _assign_characters_to_chapters(
    self,
    chapters: List[Dict],
    character_plan: Dict
) -> List[Dict]:
    """
    将角色分配到具体章节
    """
    
    for chapter in chapters:
        ch_num = chapter.get('chapter_number')
        
        # 核心角色（每章必有）
        chapter['assigned_characters'] = {
            'core': [c['name'] for c in character_plan.get('core', [])],
            'major': [],
            'minor': []
        }
        
        # 重要配角（根据首次出场章节分配）
        for char in character_plan.get('major', []):
            first_ch = char.get('first_chapter', start_chapter)
            if ch_num >= first_ch:
                chapter['assigned_characters']['major'].append(char['name'])
        
        # 次要配角
        for char in character_plan.get('minor', []):
            if ch_num == char.get('first_chapter'):
                chapter['assigned_characters']['minor'].append(char['name'])
    
    return chapters
```

---

### 第二部分: 批次开始设定回顾（一次性）

**修改文件**: `web/services/market_driven/chapter_conversation_generator.py`

**修改位置**: `_generate_batch_conversation()` 方法

```python
def _generate_batch_conversation(self, novel_title: str, start_chapter: int, 
                                 end_chapter: int, blueprint: Dict, ...):
    """批量生成章节"""
    
    # 检查是否为批次第一章
    is_batch_first = (start_chapter == 1) or (start_chapter % 6 == 1)
    
    if is_batch_first:
        # 新增：批次开始前的设定回顾
        setup_review = self._build_batch_setup_review(start_chapter, blueprint)
        
        # 插入到对话上下文（只执行一次）
        self._insert_system_message(setup_review)
    
    # 后续按原有逻辑生成...

def _build_batch_setup_review(self, start_chapter: int, blueprint: Dict) -> str:
    """构建批次设定回顾（仅第一次执行）
    """
    final_plan = self.novel_data.get('final_plan', {})
    user_choices = self.novel_data.get('user_choices', {})
    
    # 获取本批次角色规划
    characters = blueprint.get('characters', {})
    
    return f"""
📋 **批次开始设定确认** (第{start_chapter}-{start_chapter+29}章)

请确认以下设定，后续30章必须严格遵守：

## 核心设定
**主角**: {user_choices.get('protagonist_name', final_plan.get('protagonist_name', '主角'))}
- 性格: {final_plan.get('protagonist_personality', '')}
- 能力: {final_plan.get('golden_finger_summary', '')}
- 卖点: {final_plan.get('core_selling_point', '')}

## 本批次角色规划
**核心角色（必须出场）**:
{chr(10).join([f"- {c['name']}: {c.get('appearance', '')}" for c in characters.get('core', [])])}

**重要配角（根据场景出场）**:
{chr(10).join([f"- {c['name']}: 第{c.get('first_chapter', '?')}章首次出场, 作用{c.get('purpose', '')}" for c in characters.get('major', [])])}

**禁忌**:
- 禁止创造未列入规划的新角色
- 禁止让已下线角色突然出场
- 禁止修改主角核心设定

确认完毕，开始生成第{start_chapter}章...
"""
```

---

### 第三部分: 章节生成角色约束（简化）

**修改位置**: `_build_full_prompt()` 方法

```python
def _build_full_prompt(self, chapter_num: int, chapter_plan: Dict, ...) -> str:
    """构建完整的章节生成提示词
    
    简化版：不再每章加长设定，只使用角色列表
    """
    
    # 获取本章规划中的角色
    assigned_chars = chapter_plan.get('assigned_characters', {})
    
    # 简单的角色提醒（不再重复设定）
    char_reminder = f"""
### 本章角色
**必须出场**: {', '.join(assigned_chars.get('core', []))}
**建议出场**: {', '.join(assigned_chars.get('major', []))}
**可选出场**: {', '.join(assigned_chars.get('minor', []))}

**禁止**: 创造列表之外的新角色
""" if assigned_chars else ""
    
    # 原有提示词构建逻辑...
    chapter_prompt = self._build_chapter_prompt(chapter_num, chapter_plan, prev_summary)
    
    # 组合（简化版，不再加长设定）
    full_prompt = char_reminder + "\n\n" + chapter_prompt
    
    return full_prompt
```

---

## 实施计划

### 阶段一（第1周）
1. 改造 `TacticalPlanner`，新增角色规划功能
2. 实现 `_plan_characters_for_batch()` 方法
3. 实现 `_assign_characters_to_chapters()` 方法

### 阶段二（第2周）  
4. 修改 `ChapterConversationGenerator`，添加批次设定回顾
5. 简化章节生成的角色提示
6. 使用战术规划中的角色列表约束章节生成

---

## 预期效果

| 问题 | 改进前 | 改进后 |
|------|--------|--------|
| 核心设定丢失 | 频繁 | 批次开始确认后严格遵守 |
| 角色氪漫 | 每5-7章一个新角色 | 只能用30章规划好的角色 |
| 有名角色无限增长 | 无限制 | 30章最多5-8个新角色 |
| 配角被遗忘 | 常见 | 按规划出场 |
| Token浪费 | 每章长设定 | 仅批次开始一次长设定 |

---

## 与之前方案对比

| 维度 | 方案A（旧） | 方案B（新） | 方案C（本方案） |
|------|---------|---------|------------|
| **设定回顾** | 每章都加 | 每7章加 | 批次开始一次 |
| **角色管理** | 禁止新角色 | 审批新角色 | 战术规划时确定 |
| **实施复杂度** | 低 | 高 | 中 |
| **效果** | 一般 | 好但慢 | 好且快 |

---

## 待确认问题

1. **战术规划频率**: 现在是30章一次，是否合适？
2. **角色数量限制**: 30章最多5-8个新角色是否合适？
3. **角色审查**: AI自动规划还是需要用户确认？
4. **临时角色**: 是否允许AI在章节中创造无名路人？
