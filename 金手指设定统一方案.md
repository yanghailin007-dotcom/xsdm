# 金手指设定统一方案：表单模式 vs 对话打磨模式

## 一、现状分析

### 1.1 两种模式的金手指处理对比

| 维度 | 对话打磨模式 (Dialog) | 表单模式 (Form) |
|------|---------------------|----------------|
| **输入方式** | 第3轮对话选择预设选项 | 第6步表单文本输入 |
| **数据结构** | `creative_draft.golden_finger`<br>`creative_draft.golden_finger_type` | `draft.golden_finger_desc` |
| **详细程度** | ❌ 只有简单描述（如"记忆消失代价"） | ⚠️ 用户可输入详细描述，但无结构化字段 |
| **存储位置** | `project_info.json[plan][golden_finger]` | `project_info.json[plan][golden_finger_summary]` |
| **专用文件** | ❌ 无 `金手指设定.json` | ❌ 无 `金手指设定.json` |
| **章节引用** | ❌ 使用硬编码默认值 | ❌ 使用硬编码默认值 |

### 1.2 核心问题：两种模式都缺少结构化金手指设定

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          金手指数据流转图                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  【对话打磨模式】          【表单模式】            【最终产物】          │
│       ↓                       ↓                        ↓               │
│  ┌──────────┐            ┌──────────┐          ┌──────────────┐        │
│  │ 第3轮选择 │            │ 表单输入  │          │ project_info │        │
│  │          │            │          │          │              │        │
│  │ 记忆消失 │            │ SSS级全  │          │ golden_finger│        │
│  │ 代价     │            │ 民指令   │          │              │        │
│  └────┬─────┘            └────┬─────┘          └──────┬───────┘        │
│       ↓                       ↓                       ↓                │
│  creative_draft          draft.golden_finger_desc   ❌ 无统一结构      │
│  .golden_finger                                     ❌ 无专用文件        │
│       │                       │                       │                │
│       └───────────┬───────────┘                       │                │
│                   ↓                                   │                │
│           最终方案生成                                 │                │
│                   ↓                                   │                │
│         final_plan[golden_finger_summary]             │                │
│                   │                                   │                │
│                   └───────────────────────────────────┘                │
│                                   ↓                                    │
│                           ❌ 一阶段产物生成                             │
│                           ❌ 缺少金手指专用文件                         │
│                                   ↓                                    │
│                           ❌ phase_one_loader                           │
│                           ❌ 硬编码默认值                               │
│                                   ↓                                    │
│                           ❌ 章节生成使用错误金手指                      │
│                                                                        │
└─────────────────────────────────────────────────────────────────────────┘
```

### 1.3 两种模式的具体差异

#### 对话打磨模式（6轮对话）

**第1轮：差异化方向选择**
- 用户选择：主角性格 / 金手指 / 剧情反转 / 情感线 / 叙事视角

**第2轮：主角性格**
- 选择：冷静理智 / 话痨吐槽 / 佛系摆烂 / 疯批乐子 / 反英雄

**第3轮：金手指设定** ← 金手指在这里定义
```python
options = [
    {
        "id": "side_effect_memory",
        "label": "🧠 记忆消失代价",
        "description": "每次使用能力会随机遗忘一段记忆",
    },
    # ... 其他选项
]
```

**第4轮：剧情细节**

**第5轮：情感线**

**第6轮：生成方案** ← 只有简单的金手指名称，无详细结构
```json
{
  "golden_finger": "记忆消失代价",
  "golden_finger_type": "side_effect_memory"
}
```

#### 表单模式（6步表单）

**第1-5步：快速设置**
- 题材、风格、篇幅、质量、上传选项

**第6步：详细设定** ← 金手指在这里定义
```python
form_data = {
    "title": "用户书名",
    "protagonist_name": "主角名",
    "protagonist_personality": "主角性格",
    "golden_finger_desc": "SSS级全民指令整活系统，全球观众可通过弹幕发起投票指令...",  // 文本描述
    "opening_scene": "开局设计",
    "main_plot": "主线剧情",
}
```

**问题**：`golden_finger_desc` 是纯文本，无结构化字段（如名称、机制、限制、成长曲线等）

---

## 二、统一方案设计

### 2.1 统一的金手指数据模型

```python
# 统一的金手指数据模型（适用于两种模式）
class GoldenFingerDesign:
    """金手指详细设定"""
    
    # 基础信息
    name: str                    # 金手指名称（如"全民指令整活系统"）
    type: str                    # 类型ID（如"audience_interaction"）
    type_label: str              # 类型标签（如"🧠 记忆消失代价"）
    concept: str                 # 核心概念描述
    
    # 能力设定
    initial_ability: str         # 初始能力
    growth_curve: str            # 成长曲线/升级路径
    max_potential: str           # 最终形态
    
    # 限制与代价
    limitations: List[str]       # 限制条件
    side_effects: List[str]      # 副作用/代价
    cooldown_rules: str          # 冷却规则
    
    # 应用场景
    combat_usage: str            # 战斗应用
    daily_usage: str             # 日常应用
    special_mechanics: Dict      # 特殊机制
    
    # 与主角的关联
    compatibility: str           # 与主角性格的契合度说明
    combo_effects: List[str]     # 组合效果（金手指+主角性格）
    
    # 剧情作用
    plot_hooks: List[str]        # 剧情钩子（如何利用金手指制造冲突）
    twist_potential: str         # 反转潜力
    
    def to_dict(self) -> Dict:
        return {
            "name": self.name,
            "type": self.type,
            "type_label": self.type_label,
            "concept": self.concept,
            "abilities": {
                "initial": self.initial_ability,
                "growth": self.growth_curve,
                "max": self.max_potential
            },
            "restrictions": {
                "limitations": self.limitations,
                "side_effects": self.side_effects,
                "cooldown": self.cooldown_rules
            },
            "applications": {
                "combat": self.combat_usage,
                "daily": self.daily_usage,
                "special": self.special_mechanics
            },
            "protagonist_synergy": {
                "compatibility": self.compatibility,
                "combo_effects": self.combo_effects
            },
            "plot_role": {
                "hooks": self.plot_hooks,
                "twist_potential": self.twist_potential
            }
        }
```

### 2.2 产物文件统一规范

```json
// 金手指设定.json 标准格式
{
  "metadata": {
    "created_at": "2026-04-06T18:56:30",
    "created_by": "dialog_mode|form_mode",
    "version": "1.0"
  },
  "basic_info": {
    "name": "全民指令整活系统",
    "type": "audience_interaction",
    "type_label": "📺 观众互动型",
    "concept": "全球观众可通过弹幕发起投票指令。完成的指令越离谱、对怪谈造成的心理阴影越大，获得的'整活点'越高，可兑换无视规则的逆天技能。"
  },
  "abilities": {
    "initial": "初始解锁'弹幕裁判权'：可从弹幕中任选一条指令强制对象执行（成功率与对象精神状态相关）。每日限10次。",
    "growth": "前期(1-30章)：每日增长1%规则扭曲度，每10点解锁新指令权。中期(31-80章)：可对自己发布指令实现'言出法随'。后期(81-100章)：整活点可具现化'概念级武器'。",
    "max": "终极形态'位面导演'：可改写局部规则，将怪谈世界临时转化为'整活直播间'。"
  },
  "restrictions": {
    "limitations": [
      "'高级指令'每日限时使用，超时导致精神枯竭",
      "失败会积累'尴尬值'，过高时弹幕指令失效",
      "观众人数低于1000时，系统进入'低电量模式'"
    ],
    "side_effects": [
      "频繁使用会导致现实中的主角也变得话痨",
      "长期依赖弹幕决策，会削弱自主判断能力"
    ],
    "cooldown": "普通指令冷却5分钟，高级指令冷却24小时，概念级武器冷却7天"
  },
  "applications": {
    "combat": "利用弹幕指令让敌人执行尴尬动作（如强吻队友、跳钢管舞），破坏其战斗节奏",
    "daily": "通过完成搞笑指令积累整活点，兑换生活便利技能",
    "special": {
      "social_death_attack": "社死现场转化为实质攻击力",
      "audience_summon": "召唤观众'云围观'增加心理压力"
    }
  },
  "protagonist_synergy": {
    "compatibility": "与话痨吐槽型主角完美契合，主角的口才能力可放大指令效果",
    "combo_effects": [
      "话痨+整活：吐槽内容可直接转化为指令选项",
      "直播场景+弹幕系统：主场优势，效果翻倍"
    ]
  },
  "plot_role": {
    "hooks": [
      "第3章：首次使用'强吻指令'，震惊全球观众",
      "第15章：弹幕投票决定主角生死，极限反转",
      "第30章：发现系统背后真相——观众其实是..."
    ],
    "twist_potential": "系统可能是高位存在收集人类情绪的工具，主角最终要反抗'被直播'的命运"
  }
}
```

---

## 三、两种模式的适配改造

### 3.1 对话打磨模式改造

**改造第3轮：金手指选择 → 金手指详细设计**

```python
def _round_golden_finger_detailed(self, prev_choice: str, custom: str = None) -> Dict:
    """第三轮：金手指详细设定（增强版）"""
    
    # 根据用户选择的类型，展示对应的详细设定选项
    gf_templates = {
        "side_effect_memory": {
            "name": "记忆消失代价系统",
            "concept": "每次使用能力会随机遗忘一段记忆",
            "limitations": ["重要记忆可能丢失", "记忆碎片影响人格"],
            "side_effects": ["逐渐忘记自己是谁", "重要之人变得陌生"]
        },
        "unique_audience": {
            "name": "全民指令整活系统",
            "concept": "全球观众可通过弹幕发起投票指令",
            "limitations": ["需要观众人数基础", "指令不可控"],
            "side_effects": ["过度依赖外界决策", "隐私完全暴露"]
        }
    }
    
    template = gf_templates.get(prev_choice, {})
    
    # 构建AI消息：引导用户填写详细设定
    ai_message = f"""**⚡ 第三步：金手指详细设定**

当前选择：**{template.get('name', '自定义系统')}**

基础概念：{template.get('concept', '用户自定义')}

请完善以下设定（可选填，AI将根据你的描述补全）：

**1. 初始能力**
刚获得系统时，主角能做什么？
（如：每日可从弹幕中选1条指令执行）

**2. 成长曲线**
随着剧情发展，能力如何进化？
（分前期/中期/后期描述）

**3. 限制与代价**
有什么限制？使用有什么代价？
（如：{', '.join(template.get('limitations', []))}）

**4. 特殊机制**
有什么独特的玩法？
（如：社死值转化为攻击力）
"""
    
    # 设置 round_type 为 GOLDEN_FINGER_DETAILED
    round_data = DialogRound(
        round_num=3,
        round_type=DialogRoundType.GOLDEN_FINGER_DETAILED,
        ai_message=ai_message,
        options=[],  # 自由输入模式
        allow_custom=True
    )
    
    return self._format_round_response(round_data)
```

**修改最终方案生成**：将详细金手指信息包含在 `final_plan` 中

```python
def _round_generate_full_plan(self, prev_choice: str, custom: str = None) -> Dict:
    # ... 现有代码 ...
    
    # 调用AI生成金手指详细设定
    gf_design = self._generate_golden_finger_detail_with_ai()
    
    # 保存到 creative_draft
    self.creative_draft.golden_finger_design = gf_design
    
    # 构建最终方案时包含完整金手指
    full_plan = {
        # ... 其他字段 ...
        "golden_finger": {
            "name": gf_design["name"],
            "type": self.creative_draft.golden_finger_type,
            "concept": gf_design["concept"],
            "initial_ability": gf_design["abilities"]["initial"],
            "growth_curve": gf_design["abilities"]["growth"],
            "limitations": gf_design["restrictions"]["limitations"],
            "side_effects": gf_design["restrictions"]["side_effects"],
            "combat_usage": gf_design["applications"]["combat"],
            "plot_hooks": gf_design["plot_role"]["hooks"]
        }
    }
```

### 3.2 表单模式改造

**扩展第6步表单：金手指结构化输入**

```python
# 表单数据结构（扩展版）
form_data = {
    "title": "书名",
    "protagonist_name": "主角名",
    "protagonist_personality": "主角性格",
    
    # 金手指结构化字段（取代原来的 golden_finger_desc 纯文本）
    "golden_finger": {
        "name": "金手指名称",
        "type": "类型选择",
        "concept": "核心概念",
        "initial_ability": "初始能力",
        "growth_curve": "成长曲线",
        "limitations": ["限制1", "限制2"],
        "side_effects": ["副作用1", "副作用2"],
        "plot_hooks": ["剧情钩子1", "剧情钩子2"]
    },
    
    # 保留纯文本字段作为回退/补充
    "golden_finger_desc": "补充描述（可选）"
}
```

**前端表单界面改造**：

```html
<!-- 金手指详细设定表单 -->
<fieldset class="golden-finger-section">
    <legend>⚡ 金手指设定</legend>
    
    <!-- 基础信息 -->
    <div class="form-group">
        <label>金手指名称</label>
        <input type="text" name="golden_finger_name" placeholder="如：全民指令整活系统">
    </div>
    
    <div class="form-group">
        <label>类型</label>
        <select name="golden_finger_type">
            <option value="side_effect_memory">🧠 记忆消失代价</option>
            <option value="side_effect_body">💀 身体衰弱代价</option>
            <option value="restriction_name">📝 真名限制</option>
            <option value="restriction_hostility">😠 敌意触发</option>
            <option value="unique_audience">📺 观众互动型</option>
            <option value="custom">✏️ 自定义</option>
        </select>
    </div>
    
    <div class="form-group">
        <label>核心概念</label>
        <textarea name="golden_finger_concept" rows="3" 
            placeholder="一句话描述金手指的核心机制"></textarea>
    </div>
    
    <!-- 能力设定 -->
    <div class="form-group">
        <label>初始能力</label>
        <textarea name="golden_finger_initial" rows="2"
            placeholder="刚获得时有什么能力？有什么限制？"></textarea>
    </div>
    
    <div class="form-group">
        <label>成长曲线</label>
        <textarea name="golden_finger_growth" rows="3"
            placeholder="分前期/中期/后期描述能力如何进化"></textarea>
    </div>
    
    <!-- 限制与代价 -->
    <div class="form-group">
        <label>限制条件</label>
        <div class="tag-input" data-name="golden_finger_limitations"></div>
        <small>添加使用限制，如：每日限10次、需要观众基础</small>
    </div>
    
    <div class="form-group">
        <label>副作用/代价</label>
        <div class="tag-input" data-name="golden_finger_side_effects"></div>
        <small>添加副作用，如：记忆消失、身体衰弱</small>
    </div>
    
    <!-- 剧情钩子 -->
    <div class="form-group">
        <label>关键剧情节点</label>
        <textarea name="golden_finger_hooks" rows="3"
            placeholder="利用金手指设计的关键剧情（如：第3章首次强吻指令、第15章弹幕投票决定生死）"></textarea>
    </div>
</fieldset>
```

---

## 四、一阶段产物生成改造

### 4.1 新增金手指产物生成

```python
def generate_all_products(self, genre: str, tropes: Dict, plan: Dict) -> Dict:
    """生成所有第一阶段产物（含金手指）"""
    
    products = {
        # ... 现有产物 ...
        
        # 新增：10. 金手指详细设定
        "golden_finger_design": self._generate_golden_finger_product(plan),
    }
    
    return products

def _generate_golden_finger_product(self, plan: Dict) -> Dict:
    """生成金手指详细设定产物"""
    
    # 从 plan 中提取金手指信息（兼容两种模式）
    gf_data = plan.get("golden_finger", {})
    
    # 如果 plan 中只有简单描述，调用AI生成详细设定
    if not gf_data or isinstance(gf_data, str):
        gf_data = self._generate_golden_finger_with_ai(plan, gf_data)
    
    return {
        "metadata": {
            "created_at": datetime.now().isoformat(),
            "created_by": plan.get("_source", "unknown"),
            "version": "1.0"
        },
        "basic_info": {
            "name": gf_data.get("name", "未命名系统"),
            "type": gf_data.get("type", "unknown"),
            "type_label": gf_data.get("type_label", ""),
            "concept": gf_data.get("concept", "")
        },
        "abilities": {
            "initial": gf_data.get("initial_ability", ""),
            "growth": gf_data.get("growth_curve", ""),
            "max": gf_data.get("max_potential", "")
        },
        "restrictions": {
            "limitations": gf_data.get("limitations", []),
            "side_effects": gf_data.get("side_effects", []),
            "cooldown": gf_data.get("cooldown_rules", "")
        },
        "applications": {
            "combat": gf_data.get("combat_usage", ""),
            "daily": gf_data.get("daily_usage", ""),
            "special": gf_data.get("special_mechanics", {})
        },
        "protagonist_synergy": {
            "compatibility": gf_data.get("compatibility", ""),
            "combo_effects": gf_data.get("combo_effects", [])
        },
        "plot_role": {
            "hooks": gf_data.get("plot_hooks", []),
            "twist_potential": gf_data.get("twist_potential", "")
        }
    }

def _generate_golden_finger_with_ai(self, plan: Dict, simple_desc: str) -> Dict:
    """使用AI从简单描述生成详细金手指设定"""
    
    prompt = f"""基于以下金手指描述，生成详细的金手指设定。

题材：{plan.get('genre', '未知')}
主角性格：{plan.get('protagonist', {}).get('personality', '未知')}
金手指描述：{simple_desc}

请生成以下字段（JSON格式）：
- name: 金手指名称
- concept: 核心概念
- initial_ability: 初始能力
- growth_curve: 成长曲线
- limitations: 限制条件（数组）
- side_effects: 副作用（数组）
- combat_usage: 战斗应用
- plot_hooks: 剧情钩子（数组）
"""
    
    # 调用AI生成...
    response = self.api_client.generate_content_with_retry(
        content_type="golden_finger_design",
        user_prompt=prompt
    )
    
    # 解析并返回...
    return parse_golden_finger_response(response)
```

### 4.2 phase_one_loader 改造

```python
def load_golden_finger(self) -> Dict:
    """加载金手指设定（统一入口）"""
    
    # 1. 优先从专用文件读取
    gf = self._load_json("金手指设定.json", {})
    if gf:
        logger.info(f"[PhaseOneDataLoader] 从金手指设定.json加载")
        return self._normalize_golden_finger(gf)
    
    # 2. 从 project_info.json 读取（兼容旧数据）
    project_info = self._load_project_info()
    if project_info:
        plan = project_info.get("plan", {})
        gf = plan.get("golden_finger", {})
        if gf:
            logger.info(f"[PhaseOneDataLoader] 从project_info.json加载金手指")
            return self._normalize_golden_finger(gf)
        
        # 3. 尝试读取旧字段 golden_finger_summary
        summary = plan.get("golden_finger_summary", "")
        if summary:
            logger.warning(f"[PhaseOneDataLoader] 使用旧版golden_finger_summary")
            return self._create_fallback_golden_finger(summary)
    
    # 4. 从世界观读取（最低兼容）
    world = self.load_world_setting()
    power = world.get("power_system", {})
    mechanics = power.get("mechanics", {})
    mc_exclusive = mechanics.get("mc_exclusive", "")
    
    if mc_exclusive:
        logger.warning(f"[PhaseOneDataLoader] 从世界观mc_exclusive回退")
        return self._create_fallback_golden_finger(mc_exclusive)
    
    # 5. 最终回退
    logger.error(f"[PhaseOneDataLoader] 无法找到金手指设定！")
    return self._create_empty_golden_finger()

def _normalize_golden_finger(self, data: Dict) -> Dict:
    """标准化金手指数据结构"""
    
    # 处理不同版本的结构差异
    if "basic_info" in data:
        # 新结构
        return {
            "name": data["basic_info"].get("name", ""),
            "type": data["basic_info"].get("type", ""),
            "concept": data["basic_info"].get("concept", ""),
            "initial_ability": data.get("abilities", {}).get("initial", ""),
            "growth_curve": data.get("abilities", {}).get("growth", ""),
            "limitations": data.get("restrictions", {}).get("limitations", []),
            "side_effects": data.get("restrictions", {}).get("side_effects", []),
            "combat_usage": data.get("applications", {}).get("combat", ""),
            "plot_hooks": data.get("plot_role", {}).get("hooks", []),
        }
    else:
        # 旧结构/简单结构
        return data

def _create_fallback_golden_finger(self, summary: str) -> Dict:
    """从简单描述创建回退金手指对象"""
    return {
        "name": "未命名系统",
        "type": "unknown",
        "concept": summary,
        "initial_ability": "需补充",
        "growth_curve": "需补充",
        "limitations": [],
        "side_effects": [],
        "combat_usage": "",
        "plot_hooks": [],
        "_source": "fallback",
        "_note": "从简单描述生成，建议完善金手指设定"
    }
```

---

## 五、实施路线图

### 阶段1：数据层统一（优先级：高）

1. **创建金手指数据模型** (`models/golden_finger.py`)
   - 定义统一的数据结构
   - 实现序列化/反序列化方法

2. **改造 phase_one_generator.py**
   - 新增 `_generate_golden_finger_product()` 方法
   - 确保生成 `金手指设定.json` 文件

3. **改造 phase_one_loader.py**
   - 实现 `load_golden_finger()` 统一入口
   - 处理多种数据源兼容

### 阶段2：对话打磨模式增强（优先级：高）

1. **新增第3.5轮：金手指详细设定**
   - 在 `_round_golden_finger` 后增加详细设定轮
   - 收集：初始能力、成长曲线、限制、副作用等

2. **修改 `_round_generate_full_plan`**
   - 调用AI生成完整金手指结构
   - 确保 `final_plan[golden_finger]` 包含完整字段

### 阶段3：表单模式增强（优先级：中）

1. **扩展表单数据结构**
   - 将 `golden_finger_desc` 升级为结构化对象
   - 保留文本字段用于兼容

2. **前端表单界面改造**
   - 设计金手指详细设定表单
   - 增加字段说明和示例

### 阶段4：章节生成整合（优先级：高）

1. **改造 tomato_tactical_session.py**
   - 确保加载并使用 `金手指设定.json`
   - 在战术规划提示词中包含金手指细节

2. **验证章节生成**
   - 检查金手指能力表现是否符合设定
   - 验证成长曲线是否正确应用

---

## 六、验证清单

实施后验证以下场景：

### 对话打磨模式
- [ ] 第3轮选择金手指类型
- [ ] 第3.5轮填写详细设定
- [ ] 生成的 `金手指设定.json` 包含完整字段
- [ ] 章节生成时使用正确的金手指设定

### 表单模式
- [ ] 表单可输入结构化金手指信息
- [ ] 生成的 `金手指设定.json` 与对话模式格式一致
- [ ] 章节生成时使用正确的金手指设定

### 数据兼容性
- [ ] 旧项目（无金手指设定.json）可正常回退
- [ ] 两种模式生成的数据结构可互换
- [ ] phase_one_loader 正确识别所有数据源

---

## 七、结论

### 核心改进点

1. **统一数据模型**：建立标准的金手指数据结构，两种模式输出一致
2. **专用产物文件**：新增 `金手指设定.json`，与 `世界观设定.json` 分离
3. **详细程度提升**：从简单名称升级为包含能力、限制、成长曲线的完整设定
4. **双向兼容**：
   - 简单模式（旧数据）→ 自动生成/回退
   - 详细模式（新数据）→ 完整使用

### 预期效果

| 指标 | 改造前 | 改造后 |
|------|--------|--------|
| 金手指详细程度 | 只有名称 | 完整结构（8个维度） |
| 章节生成引用 | 硬编码默认值 | 使用用户设定 |
| 设定一致性 | ❌ 前后矛盾 | ✅ 统一引用 |
| 用户体验 | ❌ 设定被忽视 | ✅ 设定被尊重 |

### 立即行动项

1. **今天**：创建 `金手指设定.json` 的数据模型和生成逻辑
2. **本周**：改造 `phase_one_loader.py` 实现统一加载
3. **下周**：增强对话模式第3轮，收集详细金手指信息
4. **下周**：扩展表单模式，支持结构化输入

---

*方案版本：v1.0*
*创建时间：2026-04-06*
*适用范围：市场导向模式的两种输入方式*
