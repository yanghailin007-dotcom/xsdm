# 市场导向磨托模式 - 用户创意修改丢失问题分析报告

## 问题描述

用户在对话磨托过程中修改了创意方向（如：主角和二哈宠物一起毒舌、搞子人、搞人形态），但最终生成的内容仍然是旧的设定（白月魁）。

---

## 根本原因

### 1. 第四轮 `_round_plot_details` - 开局设计选择丢失 ⚠️严重 Bug

**位置**: `web/services/market_driven/dialog_polish_manager.py:406-474`

**问题代码**:
```python
def _round_plot_details(self, prev_choice: str, custom: str = None) -> Dict:
    """第四轮：剧情细节"""
    # 只记录了金手指，没有记录开局设计选择！！！
    self.creative_draft.golden_finger = gf_map.get(prev_choice, prev_choice)
    if custom:
        self.creative_draft.golden_finger += f"（自定义：{custom}）"
    
    # 缺失：应该记录开局设计选择到 draft.opening_design
```

**影响**: 用户在第四轮选择的开局设计（classic/unexpected/revenge/forced）和自定义输入（如"二哈宠物一起毒舌"）完全丢失。

---

### 2. 第五轮 `_round_emotion_line` - 字段错误 ⚠️严重 Bug

**位置**: `web/services/market_driven/dialog_polish_manager.py:476-541`

**问题代码**:
```python
def _round_emotion_line(self, prev_choice: str, custom: str = None) -> Dict:
    """第五轮：情感线"""
    # 错误：把情感线记录到了 opening_design 字段！！！
    self.creative_draft.opening_design = prev_choice  # 这是错的！
    if custom:
        self.creative_draft.opening_design += f"（自定义：{custom}）"
```

**影响**: 
- 情感线选择覆盖了开局设计
- `opening_design` 字段存储的是情感线而不是开局设计
- 第四轮的开局设计完全丢失

---

### 3. 第二轮 `_round_protagonist_type` - 自定义输入丢失

**位置**: `web/services/market_driven/dialog_polish_manager.py:259-326`

**问题代码**:
```python
def _round_protagonist_type(self, prev_choice: str, custom: str = None) -> Dict:
    """第二轮：主角性格选择"""
    # 只记录了选择 ID，没有记录自定义输入！！！
    self.creative_draft.protagonist_type = prev_choice
    # 缺失：应该追加 custom 到 protagonist 字段
```

**影响**: 用户在第二轮输入的自定义主角设定（如"主角和二哈宠物一起毒舌"）没有被追加到 draft。

---

### 4. AI 生成阶段 - 自定义输入完全丢失

**位置**: `web/services/market_driven/dialog_polish_manager.py:667-713`

**问题代码**:
```python
def _generate_plan_with_ai(self) -> Dict:
    """调用AI生成完整方案"""
    prompt = f"""请基于以下设定，生成一个完整的创作方案：

**题材：** {self.genre}
**主角：** {self.creative_draft.protagonist}  # 只使用了基础设定，没有各轮次的自定义输入
**金手指：** {self.creative_draft.golden_finger}
**情感线：** {self.creative_draft.emotion_line}
**差异化：** {self.creative_draft.unique_points}
"""
    # 缺失：没有收集各轮次的 user_custom_input 并传递给 AI
```

**影响**: 即使各轮次记录了自定义输入，AI 生成方案时也没有使用这些输入。

---

## 数据流图解析

```
用户选择流程：

第一轮：选择题材类型
├──选择：国运文-直播类
└──记录到：draft.genre ✓

第二轮：选择主角性格  ← 自定义输入丢失！
├──选择：talkative（话瘘吐槽型）
├──自定义："主角和二哈宠物一起毒舌，搞子人"
├──记录到：round_data.user_choice = "talkative" ✓
├──记录到：round_data.user_custom_input = "自定义内容" ✓
└──缺失：没有追加到 draft.protagonist ❌

第三轮：选择金手指
├──选择：unique_audience（观众互动型）
├──自定义："二哈也能看见弹幕"
└──记录到：draft.golden_finger = "观众互动型（自定义：二哈也能看见弹幕）" ✓

第四轮：选择开局设计  ← 完全丢失！
├──选择：unexpected（意外入局）
├──自定义："主角被迫带着二哉参加"
├──记录到：round_data.user_choice = "unexpected" ✓
├──记录到：round_data.user_custom_input = "自定义内容" ✓
└──缺失：没有保存到 draft 任何字段 ❌❌❌

第五轮：选择情感线  ← 字段错误！
├──选择：pet（宠物羁绊）
├──自定义："二哈会吐槽主角"
└──错误记录到：draft.opening_design = "pet（自定义：二哈会吐槽主角）" ❌
    应该记录到：draft.emotion_line

第六轮：AI生成完整方案
├──调用 _generate_plan_with_ai()
├──使用的数据：
│   ├──draft.protagonist = "话瘘吐槽型" （缺少自定义部分）
│   ├──draft.golden_finger = "观众互动型（自定义...）" ✓
│   ├──draft.opening_design = "pet（自定义...）" （错误的情感线）
│   └──draft.emotion_line = "" （未设置）
└──AI 生成结果：仍然是旧设定（白月魁）
```

---

## 具体修复方案

### 修复 1：第四轮记录开局设计

```python
def _round_plot_details(self, prev_choice: str, custom: str = None) -> Dict:
    """第四轮：剧情细节"""
    # 记录金手指选择
    gf_map = {...}
    self.creative_draft.golden_finger = gf_map.get(prev_choice, prev_choice)
    self.creative_draft.golden_finger_type = prev_choice
    if custom:
        self.creative_draft.golden_finger += f"（自定义：{custom}）"
    
    # ✅ 新增：记录开局设计选择
    # 注意：需要在这里或process_user_input中记录上一轮的选择
```

### 修复 2：第五轮正确记录情感线

```python
def _round_emotion_line(self, prev_choice: str, custom: str = None) -> Dict:
    """第五轮：情感线"""
    # ✅ 修复：先保存上一轮的开局设计选择
    opening_map = {...}
    self.creative_draft.opening_design = opening_map.get(prev_choice, prev_choice)
    if custom:
        self.creative_draft.opening_design += f"（自定义：{custom}）"
    
    # 然后返回情感线选项...
```

### 修复 3：第二轮追加自定义输入

```python
def _round_protagonist_type(self, prev_choice: str, custom: str = None) -> Dict:
    """第二轮：主角性格选择"""
    # 记录选择
    self.creative_draft.protagonist_type = prev_choice
    
    # ✅ 新增：追加自定义输入到 protagonist
    if custom:
        if not self.creative_draft.protagonist:
            self.creative_draft.protagonist = ""
        self.creative_draft.protagonist += f"（自定义：{custom}）"
```

### 修复 4：AI 生成时收集所有自定义输入

```python
def _generate_plan_with_ai(self) -> Dict:
    # ✅ 新增：收集各轮次的自定义输入
    custom_inputs = []
    for round_data in self.rounds:
        if round_data.user_custom_input:
            custom_inputs.append(f"第{round_data.round_num}轮：{round_data.user_custom_input}")
    
    all_custom = "\n".join(custom_inputs)
    
    prompt = f"""请基于以下设定，生成一个完整的创作方案：

**题材：** {self.genre}
**主角：** {self.creative_draft.protagonist}
**金手指：** {self.creative_draft.golden_finger}
**情感线：** {self.creative_draft.emotion_line}
**差异化：** {self.creative_draft.unique_points}

**🔥 用户自定义创意修改（必须严格遵守）：**
{all_custom}

请根据上述用户的自定义修改，生成符合要求的方案。
"""
```

---

## 临时解决方案

在代码修复前，建议用户使用**快速表单模式**，直接在表单中填写详细的创意设定，避免对话模式中的数据丢失问题。

---

## 总结

问题的核心是：**用户在对话过程中的自定义输入没有被正确保存和传递到AI生成阶段**。

具体表现为：
1. 第二轮自定义输入未追加到 draft
2. 第四轮开局设计选择完全未保存
3. 第五轮情感线错误保存到 opening_design
4. AI 生成时未使用各轮次的 user_custom_input

建议优先级：P1（严重影响用户体验）
