# 文风系统实施方案报告 - 最终流程修正

## 流程修正说明

经用户最终确认，文风选择的正确位置是：

### ✅ 最终正确流程

**市场导向模式：**
```
题材分析 
  ↓
快速表单/AI打磨（用户输入阶段结束）
  ↓
【文风选择弹窗】 ← 所有用户配置完成后，AI生成开始前
  ↓
生成世界观（注入文风）
  ↓
生成细纲（使用选定文风）
  ↓
生成黄金三章（使用选定文风）
  ↓
如开启对照组：显示版本选择 → 用户确认正式版
  ↓
第4章+（继承文风，可切换）
```

**自由创意模式：**
```
输入创意
  ↓
填写表单（用户输入阶段结束）
  ↓
【文风选择弹窗】 ← 所有用户配置完成后，AI生成开始前
  ↓
一阶段生成（设定+世界观+细纲，使用选定文风）
  ↓
二阶段生成（章节，使用选定文风）
  ↓
如开启对照组：显示版本选择 → 用户确认正式版
  ↓
后续章节（继承文风，可切换）
```

---

## 核心原则（最终版）

```
文风选择 = 用户完成所有配置后，AI开始正式生成前的最后一步

即：
【用户输入阶段结束】→【文风选择】→【AI生成阶段开始】
```

### 为什么放在这里？

| 位置 | 是否合适 | 原因 |
|-----|---------|------|
| 题材分析后 | ❌ 太早 | 用户还没输入任何设定 |
| **表单完成后** | ✅ **正确** | **用户输入全部完成，即将开始AI生成** |
| 世界观生成后 | ⚠️ 太晚 | 世界观的叙述也应该带文风 |
| 黄金三章后 | ❌ 太晚 | 黄金三章必须使用文风 |

---

## 文风应用范围（最终版）

选定文风后，影响的生成环节：

| 生成环节 | 市场导向 | 自由创意 |
|---------|---------|---------|
| 世界观生成 | ✅ 使用文风 | ✅ 使用文风（在一阶段） |
| 细纲生成 | ✅ 使用文风 | ✅ 使用文风（在一阶段） |
| 黄金三章 | ✅ 强制使用 | ✅ 强制使用 |
| 第4章+ | ✅ 继承，可切换 | ✅ 继承，可切换 |
| 对照组 | ✅ 黄金三章支持 | ✅ 黄金三章支持 |

---

## 技术触发点（最终版）

```python
# 统一触发逻辑
def should_show_style_selector(project_type, current_step):
    triggers = {
        # 市场导向：表单完成后，世界观生成前
        "market_driven": {
            "trigger_after": "user_forms_completed",
            "trigger_before": "worldview_generation",
            "condition": "user_confirmed_inputs"
        },
        
        # 自由创意：表单完成后，一阶段生成前
        "free_creative": {
            "trigger_after": "form_completed", 
            "trigger_before": "phase_one_generation",
            "condition": "user_confirmed_creation"
        },
    }
```

---

## 与黄金三章的对接

```
用户选择文风
    ↓
系统保存：project.writing_style.selected_style_id
    ↓
生成世界观（注入文风Prompt）
    ↓
生成细纲（使用文风）
    ↓
生成黄金三章（强制使用文风）
    ↓
如开启对照组：
    - 并行生成版本A（用户文风）
    - 并行生成版本B（标准文风/对照文风）
    ↓
用户在对照组界面选择"正式版本"
    ↓
被选中的版本文风 → 自动应用到后续章节
```

---

## 总结

**文风选择 = 用户输入结束 → AI生成开始的临界点**

- 市场导向：**表单完成后** → 文风选择 → 世界观生成
- 自由创意：**表单完成后** → 文风选择 → 一阶段生成

这样确保从世界观开始，所有AI生成的内容都带有选定的文风特征。
