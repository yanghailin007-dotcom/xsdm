# 章节生成提示词固化问题分析报告

## 一、问题发现

### 1.1 观察到的现象

从debug文件 `api_call_CCG-开局带只二哈我直播气-F69FA7E2_第1章_20260406_135145_attempt1.txt` 中发现：

**提示词中的固定模板内容：**
```
[角色设定提醒]
[主角名]沈浪(必须严格使用此名字,禁止用其他名字)
[核心特质]极致理智, 护短爱国, 杀伐果断  ✅ 正确（来自角色设计.json）

【世界状态约束 - 必须遵循】

主角(沈浪)当前状态:
  - 健康: 健康
  - 已解锁能力: 基础雷电操控  ❌ 错误！这是另一本书的设定

系统规则(扮演度):
  - 当前扮演度: 0.0%  ❌ 错误！应该是"弹幕干涉系统"
  - 历史最高: 0.0%
  - 已解锁技能: 静电操控  ❌ 错误！

活跃剧情线索(本章需要提及或推进):
  - 反龙联盟: 漂亮国牵头的反龙国联盟  ❌ 错误！这是另一本书的剧情
```

### 1.2 AI生成的错误内容

由于提示词的错误引导，AI生成了完全偏离设定的内容：

```
【系统提示：检测到宿主沈浪处于极致理智状态（醉酒加成），扮演度系统激活！】
【当前扮演角色：雷电法王（整活版）。】  ❌ 应该是"弹幕干涉系统"
【已解锁能力：基础雷电操控（静电级）。】  ❌ 应该是"逻辑改写"

...

沈浪伸出手指，指尖跳动着一丝微弱的电火花。  ❌ 应该是弹幕提示
【系统提示：宿主沈浪尝试操控静电，扮演度+0.1%。】  ❌ 应该是弹幕互动热度
```

**完全偏离了"开局带只二哈"的核心设定：**
- ❌ 应该是"弹幕干涉系统" → AI写的是"扮演度系统"
- ❌ 应该是"逻辑改写能力" → AI写的是"雷电操控"
- ❌ 应该是"二哈拆家+直播整活" → AI写的是"雷电法王"

---

## 二、根本原因分析

### 2.1 问题定位

```
┌─────────────────────────────────────────────────────────────────┐
│                     提示词生成流程                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  一阶段产物（正确）                                              │
│  ├─ 角色设计.json → [核心特质]极致理智,护短爱国,杀伐果断 ✅     │
│  ├─ 世界观设定.json → 【弹幕干涉系统】【逻辑改写】              │
│  └─ 升级路线.json → 【1-30级:弹幕提示范围每级+100米】          │
│                                                                 │
│           ↓ 读取                                                  │
│                                                                 │
│  ┌──────────────────────────────────────────────┐              │
│  │   world_state.json（状态存储）                │              │
│  │   ├─ 已解锁能力: 基础雷电操控  ← 残留数据！   │              │
│  │   ├─ 系统规则(扮演度)          ← 残留数据！   │              │
│  │   ├─ 已解锁技能: 静电操控      ← 残留数据！   │              │
│  │   └─ 活跃剧情线索: 反龙联盟    ← 残留数据！   │              │
│  └──────────────────────────────────────────────┘              │
│                                                                 │
│           ↓ 优先使用                                              │
│                                                                 │
│  章节生成提示词（错误）                                          │
│  ├─ [核心特质] → 从角色设计.json读取 ✅                        │
│  ├─ [已解锁能力] → 从world_state.json读取 ❌                   │
│  ├─ [系统规则] → 从world_state.json读取 ❌                     │
│  └─ [剧情线索] → 从world_state.json读取 ❌                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 核心问题

**问题1：World State 残留数据污染**
- `world_state.json` 中存储的是之前项目（或其他书）的状态数据
- 当前代码优先读取 `world_state.json`，而不是根据 `phase_one_products` 动态生成

**问题2：缺乏动态初始化机制**
- 新项目启动时，`world_state.json` 没有被清空或根据一阶段产物重新初始化
- 导致旧数据持续影响新项目的章节生成

**问题3：提示词构建逻辑缺陷**
- 章节生成器的 `_build_world_state_prompt()` 等方法直接从 `world_state.json` 读取
- 没有优先使用 `phase_one_products` 中的一阶段设定

---

## 三、问题影响范围

### 3.1 受影响的提示词部分

| 提示词部分 | 当前来源 | 应该来源 | 影响程度 |
|-----------|---------|---------|---------|
| [核心特质] | 角色设计.json | 角色设计.json | ✅ 正确 |
| [已解锁能力] | world_state.json | 世界观设定.json（金手指） | 🔴 严重错误 |
| [系统规则] | world_state.json | 世界观设定.json（金手指机制） | 🔴 严重错误 |
| [已解锁技能] | world_state.json | 升级路线.json（当前阶段） | 🔴 严重错误 |
| [剧情线索] | world_state.json | 阶段目标.json（当前目标） | 🟡 中等错误 |
| [世界状态] | world_state.json | 应该动态生成或从章节总结提取 | 🟡 中等偏差 |

### 3.2 导致的后果

1. **设定崩坏**：所有生成的章节都基于错误的能力系统（雷电操控 vs 弹幕改写）
2. **剧情混乱**：剧情线索来自另一本书（反龙联盟 vs 国运禁地）
3. **风格不一致**：主角能力忽左忽右，读者体验极差
4. **无法完结**：基于错误设定生成的剧情无法与大纲对齐

---

## 四、代码层面的问题定位

### 4.1 可疑的代码位置

需要检查以下文件中的提示词构建逻辑：

1. **`chapter_conversation_generator.py`**
   - `_build_world_state_prompt()` 方法
   - `_build_system_prompt()` 方法
   - `_build_user_prompt()` 方法

2. **`chapter_prompt_optimizer_v3.py` 或类似文件**
   - `build_prompt()` 方法
   - `get_world_state_context()` 方法

3. **`world_state_manager.py`（如果存在）**
   - `get_current_state()` 方法
   - `initialize_from_phase_one()` 方法

### 4.2 问题代码模式

当前的问题代码可能是这样：

```python
def _build_world_state_prompt(self):
    # ❌ 错误：直接从 world_state.json 读取
    world_state = self._load_world_state()
    
    prompt = f"""
【世界状态约束 - 必须遵循】

主角({protagonist_name})当前状态:
  - 健康: {world_state.get('health', '健康')}
  - 已解锁能力: {world_state.get('unlocked_abilities', [])}  ← 可能是旧数据

系统规则({world_state.get('system_name', '扮演度')}):  ← 可能是旧数据
  - 当前扮演度: {world_state.get('role_playing', 0)}%
  - 已解锁技能: {world_state.get('skills', [])}  ← 可能是旧数据

活跃剧情线索(本章需要提及或推进):
{world_state.get('active_plots', [])}  ← 可能是旧数据
"""
    return prompt
```

---

## 五、修复方案

### 5.1 短期修复（立即生效）

**方案A：清空 world_state.json 中的固化数据**

```python
# 在生成章节提示词前，强制从 phase_one_products 重新加载
def _build_world_state_prompt(self):
    # ✅ 优先从一阶段产物加载
    phase_one = PhaseOneDataLoader(self.project_path).load_all()
    
    # 获取金手指设定
    golden_finger = phase_one.get('world_setting', {}).get('power_system', {})
    
    # 获取当前阶段
    stage_goal = self._get_current_stage_goal()
    
    prompt = f"""
【核心设定约束 - 必须遵循】

### 世界观设定
{background}

### 金手指详细规则（必须严格遵守）
系统名称: {golden_finger.get('name', '未设定')}
核心机制: {golden_finger.get('mechanics', '未设定')}
触发条件: {golden_finger.get('trigger', '未设定')}
当前阶段: {self._get_current_power_stage()}

### 当前阶段目标
{stage_goal.get('description', '未设定')}
关键交付物: {stage_goal.get('key_deliverables', [])}

### 主角当前状态（第1章初始状态）
- 身份: {protagonist.get('identity', '普通人')}
- 能力: 初始F级（未解锁金手指）
- 状态: 等待系统觉醒

⚠️ 禁止：使用任何未在"金手指详细规则"中定义的能力
⚠️ 禁止：偏离"核心机制"描述的系统规则
"""
    return prompt
```

### 5.2 中期修复（架构优化）

**方案B：创建 WorldStateInitializer 类**

```python
class WorldStateInitializer:
    """
    根据一阶段产物初始化世界状态
    确保新项目不会受到旧数据污染
    """
    
    def initialize_from_phase_one(self, project_path: Path) -> Dict:
        """从一阶段产物生成初始世界状态"""
        phase_one = PhaseOneDataLoader(project_path).load_all()
        
        return {
            'protagonist': {
                'name': self._get_protagonist_name(phase_one),
                'health': '健康',
                'power_level': 'F级（初始）',
                'unlocked_abilities': [],  # 初始无能力
            },
            'system': {
                'name': self._get_system_name(phase_one),  # 从金手指读取
                'mechanics': self._get_system_mechanics(phase_one),
                'activation_condition': self._get_activation_condition(phase_one),
                'current_level': 0,
                'unlocked_skills': [],  # 初始无技能
            },
            'active_plots': self._get_initial_plots(phase_one),  # 从阶段目标读取
            'global_state': {
                'national_support': 10,  # 初始支持率低
                'foreign_attitude': '嘲讽',
            }
        }
```

### 5.3 长期修复（设计文档）

**方案C：重新设计状态管理架构**

```
新架构：

PhaseOneProducts (一阶段产物 - 静态设定)
    │
    ▼
WorldStateInitializer (初始化器)
    │
    ▼
WorldState (世界状态 - 动态数据)
    │
    ▼
ChapterGenerator (章节生成器)

规则：
1. WorldState 的初始值必须从 PhaseOneProducts 生成
2. WorldState 只能存储"动态变化"的数据（如当前伤势、最新解锁技能）
3. WorldState 不能存储"静态设定"（如系统名称、核心机制）
4. 章节生成提示词 = PhaseOneProducts（静态）+ WorldState（动态）
```

---

## 六、验证方法

### 6.1 测试用例

创建一个测试来验证修复效果：

```python
def test_chapter_prompt_uses_phase_one_data():
    """测试章节生成提示词使用一阶段产物数据"""
    
    # 加载一阶段产物
    phase_one = PhaseOneDataLoader(project_path).load_all()
    
    # 生成第1章提示词
    generator = ChapterConversationGenerator(...)
    prompt = generator.build_prompt(chapter_num=1)
    
    # 验证：提示词中不能出现 world_state.json 中的错误数据
    assert "基础雷电操控" not in prompt, "错误：提示词包含残留数据'基础雷电操控'"
    assert "扮演度系统" not in prompt, "错误：提示词包含残留数据'扮演度系统'"
    assert "反龙联盟" not in prompt, "错误：提示词包含残留数据'反龙联盟'"
    
    # 验证：提示词必须包含一阶段产物中的正确数据
    assert "弹幕干涉系统" in prompt, "错误：提示词缺少正确的金手指名称"
    assert "逻辑改写" in prompt, "错误：提示词缺少正确的核心机制"
```

---

## 七、行动计划

| 优先级 | 任务 | 负责人 | 预计时间 |
|--------|------|--------|---------|
| 🔴 P0 | 定位并修复 `_build_world_state_prompt()` 方法 | - | 2小时 |
| 🔴 P0 | 清空/修复现有项目的 world_state.json | - | 30分钟 |
| 🟡 P1 | 创建 WorldStateInitializer 类 | - | 4小时 |
| 🟡 P1 | 添加测试用例防止回归 | - | 2小时 |
| 🟢 P2 | 重新设计状态管理架构文档 | - | 4小时 |

---

## 八、结论

**核心问题**：章节生成提示词从 `world_state.json` 读取数据，而该文件包含之前项目的残留数据，导致提示词中的设定与一阶段产物不符。

**影响**：所有生成的章节都基于错误的设定（雷电操控 vs 弹幕改写），导致设定崩坏、剧情混乱。

**解决方案**：
1. 短期：修改提示词构建逻辑，优先从 `phase_one_products` 读取
2. 中期：创建初始化器，确保新项目从正确来源生成初始状态
3. 长期：重新设计架构，分离"静态设定"和"动态状态"

---

*报告生成时间: 2026-04-06*  
*分析对象: 章节生成提示词固化问题*
