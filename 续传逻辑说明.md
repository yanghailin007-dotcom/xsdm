# 定时发布三种场景逻辑说明

## 场景一：首次发布（新书）

**触发条件**：`publish_config` 为空，或 `publish_config` 中没有配置

**逻辑**：
1. 使用默认配置：
   - `first_publish_count = 20`（前20章今天发布）
   - `daily_count = 8`（之后每天8章）
   - `publish_time = '07:00'`（发布时间）
2. 从**明天**开始计算定时

**示例**：
```
今天4月7日，上传100章
- 第1-20章：今天立即发布
- 第21-28章：4月8日定时
- 第29-36章：4月9日定时
- ...
```

---

## 场景二：自动续传

**触发条件**：`publish_config` 存在，但没有 `manual_publish_date` 和 `manual_publish_time`

**逻辑**：
1. 从番茄平台同步最后发布时间
2. 计算今天已发布章节数
3. **智能判断续传起点**：

### 情况2a：今天已满额
```python
if today_published_count >= daily_count:
    start_date = 明天
    # 从明天开始新的发布周期
```

### 情况2b：最后发布是昨天或更早
```python
if last_published_time.date() < now.date():
    start_date = 今天
    # 从今天开始，使用今天第一个时间点
```

### 情况2c：今天部分发布
```python
# 今天已发布X章，继续从今天的时间点开始
# 记录已使用时间点的使用情况
```

**示例**：
```
昨天发布了第1-20章
今天4月7日继续上传第21-100章
→ 从今天的第一个时间点开始

如果今天已经发布了8章（满额）
→ 从明天开始
```

---

## 场景三：手动配置续传

**触发条件**：`publish_config.manual_publish_date` 和 `publish_config.manual_publish_time` 存在

**逻辑**：
1. 完全按照用户指定的时间开始
2. 从 `manual_date manual_time` 开始分配
3. 每天 `daily_count` 章，章节间隔30分钟

**示例**：
```
manual_date = "2026-04-10"
manual_time = "09:00"
daily_count = 8

- 第21-28章：4月10日 09:00, 09:30, 10:00...
- 第29-36章：4月11日 09:00, 09:30, 10:00...
- ...
```

---

## 修复记录

### 修复1：默认配置（首次发布）
**文件**：`main.py`
**问题**：配置为空时直接返回，所有章节立即发布
**修复**：配置为空时使用默认番茄签约配置

### 修复2：自动续传逻辑
**文件**：`main.py`
**问题**：
- 今天已满额时仍从今天继续
- 昨天发布过，今天从零开始时逻辑混乱

**修复后逻辑**：
```python
if today_published_count >= daily_count:
    # 今天已满，从明天开始
    start_date = now.date() + timedelta(days=1)
elif last_published_time.date() < now.date():
    # 昨天发布的，从今天开始（已发0章）
    start_date = now.date()
else:
    # 今天部分发布，继续今天
    # 记录已使用情况
```

---

## 配置优先级

1. **手动配置**（最高优先级）
   - `manual_publish_date` + `manual_publish_time`
   
2. **自动续传**
   - 从平台同步数据
   
3. **首次发布默认配置**（最低优先级）
   - `first_publish_count = 20`
   - `daily_count = 8`
   - `publish_time = '07:00'`

---

## 使用建议

### 新书首次发布
- 不需要特殊配置，使用默认即可
- 前20章今天发，之后每天8章

### 断点续传
- 一般不需要手动配置
- 程序会自动从平台同步已发布数据
- 智能判断从哪天继续

### 特殊情况（手动配置）
- 想指定从某个特定日期开始
- 想跳过某些天的发布
- 手动配置 `manual_publish_date` 和 `manual_publish_time`

---

构建时间：2026/4/7 10:34:53
