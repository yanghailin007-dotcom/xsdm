# V2 六层架构集成指南

## 当前进度

已完成：
1. ✅ 架构设计文档 (`docs/V2_六层架构完整设计.md`)
2. ✅ 题材分离实现 (`genre_techniques/`)
3. ✅ 网文情绪曲线 (`emotion_curves.yaml`)

待实现：
1. ⏳ 各层 Loader
2. ⏳ 组装引擎
3. ⏳ 与现有系统集成

## 快速集成方案

### 方案：渐进式替换（推荐）

不需要一次性重写所有代码，可以逐步替换：

#### 第1步：替换题材技法层（立即生效）

修改 `chapter_conversation_generator.py`：

```python
# 在 _build_system_prompt 方法中
from web.services.market_driven.prompt_assembler import PromptAssembler

# 替换原有的题材技法生成
# 原代码：
# genre_guide = self._build_genre_specific_guide()

# 新代码：
assembler = PromptAssembler(self.genre)  # self.genre 从 novel_data 获取
genre_guide = assembler._build_layer3_genre_techniques()
```

#### 第2步：替换情绪曲线（立即生效）

修改章节生成参数：

```python
# 原代码：
emotion_curve = "起-承-转-合"

# 新代码：
emotion_plan = {
    "chapter_type": "打脸章",  # 从 tactical_plan 获取
    "curve": "虐(4)→急(7)→爽(9)→悬(7)"
}
```

#### 第3步：完整迁移（后续迭代）

等V2架构完全稳定后，再完整替换。

---

## 立即可用的改进

### 1. 修复情绪曲线

已在 `prompt_packages/_base/system_components/chapter_stage.json` 中修复：

```json
"default_values": {
  "emotion_curve": "虐(4)→急(7)→爽(9)→悬(7) - 网文打脸章节奏，严禁起承转合"
}
```

### 2. 题材分离已完成

```python
from web.services.market_driven.prompt_assembler import PromptAssembler

# 国运文
assembler = PromptAssembler("国运文")
print(assembler.genre_techniques.forbidden_elements)  # 包含禁用金钱交易

# 神豪文  
assembler = PromptAssembler("神豪文")
print(assembler.genre_techniques.forbidden_elements)  # 包含禁用国运直播
```

### 3. 使用新的提示词组装

```python
from web.services.market_driven.prompt_assembler import assemble_prompt

prompt = assemble_prompt(
    genre="神豪文",
    novel_title="开局消费百亿",
    chapter_num=7,
    protagonist_name="陈默",
    chapter_type="收获章"  # 新的情绪曲线
)
```

---

## 完整V2实现（如果需要）

如需完整实现六层架构，需要创建：

```
web/services/market_driven/v2_architecture/
├── __init__.py
├── models.py                    # 数据模型
├── layer_loaders.py             # 各层加载器
├── prompt_assembler_v2.py       # 组装引擎
└── renderers.py                 # 模板渲染器
```

预计工作量：2-3天

---

## 建议

**当前建议**：使用已有的改进（题材分离+情绪曲线修复），立即解决：
1. 神豪文写出国运文的问题
2. 起承转合节奏问题

**后续迭代**：根据实际效果，决定是否完整迁移到V2架构。

是否继续完整实现V2架构？还是先用现有改进？
