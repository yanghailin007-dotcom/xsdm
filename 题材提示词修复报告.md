# 题材提示词系统性修复报告

## 问题根源

所有小说变成国运文写法的根本原因是：**提示词模板静态硬编码，不随题材变化**。

### 核心问题文件

1. **`format_rules.json`** - 硬编码"国运文专用"弹幕要求
2. **`chapter_stage.json`** - 硬编码国运文直播弹幕要求  
3. **`genre_specific_guide.json`** - 硬编码国运文弹幕模板
4. **`trope_prompt_builder.py`** - 多处硬编码国运文默认值

## 修复内容

### 1. 多题材模板架构

将单模板改为多模板结构：

```json
{
  "templates": {
    "国运文": "...",
    "神豪文": "...",
    "通用": "..."
  },
  "default_template": "通用"
}
```

### 2. 修复的文件列表

| 文件 | 修复内容 |
|------|----------|
| `format_rules.json` | 添加国运文/神豪文/通用三个模板 |
| `chapter_stage.json` | 添加国运文/神豪文/通用三个模板 |
| `genre_specific_guide.json` | 添加多题材模板支持 |
| `trope_prompt_builder.py` | 根据genre动态选择模板 |
| `chapter_prompt_optimizer_v3.py` | 支持多模板渲染 |

### 3. 关键修复点

#### 国运文模板
- ✅ 弹幕格式（【龙国观众：...】）
- ✅ 每章8-10条弹幕要求
- ✅ 直播反应链

#### 神豪文模板  
- ✅ 金额精确到小数点后2位
- ✅ 系统提示音（【叮！恭喜宿主...】）
- ✅ 路人心理活动模板
- ✅ 无弹幕要求

## 如何验证修复

1. **创建神豪文项目**
   ```
   题材：神豪文-花钱返利类
   标题：开局消费百亿，我成了首富
   ```

2. **检查系统提示词**
   查看 `debug_responses/api_call_*.txt` 中的 System Prompt：
   
   **修复前**：
   ```
   **B. 弹幕格式（国运文必须用【】包裹）：**
   **D. 直播弹幕密度要求（国运文）：**
   - 每章至少8-10条弹幕
   ```
   
   **修复后**：
   ```
   **神豪文金钱规范（核心要求）**
   - 所有金额必须精确到小数点后2位
   **系统提示音模板**：
   【叮！恭喜宿主消费XXX元，返利XXX元已到账！】
   ```

3. **生成章节验证**
   - 神豪文不应出现【龙国观众】弹幕
   - 神豪文应有精确金额（如：9876543.21元）
   - 神豪文应有消费返利提示音

## 后续优化建议

### 1. 其他需要修复的组件

以下组件仍包含硬编码国运文内容，建议逐步修复：

- `depressing_chapter_enhancement.json` - 压抑章节增强
- `chapter_expansion_prompts.json` - 章节扩写提示
- `emotion_quality_standards.json` - 情绪质量标准
- `auto_fix_triggers.json` - 自动修复触发器

### 2. 系统性设计方案

```
prompt_packages/
├── _base/
│   └── system_components/
│       ├── chapter_stage/          # 按题材分子目录
│       │   ├── 国运文.json
│       │   ├── 神豪文.json
│       │   └── 通用.json
│       └── format_rules/
│           ├── 国运文.json
│           ├── 神豪文.json
│           └── 通用.json
```

### 3. 动态模板变量方案

使用模板引擎（如 Jinja2）：

```json
{
  "template": "{% if genre == '国运文' %}\n弹幕要求：每章8-10条\n{% elif genre == '神豪文' %}\n金钱规范：精确到分\n{% endif %}"
}
```

## 测试检查清单

- [ ] 创建神豪文项目，System Prompt 不包含【龙国观众】
- [ ] 创建神豪文项目，System Prompt 包含金额精确要求
- [ ] 创建国运文项目，System Prompt 包含弹幕格式要求
- [ ] 生成章节内容符合对应题材特征
