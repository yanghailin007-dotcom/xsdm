# 金手指设定简化方案（不新增步骤，AI自动补全）

## 核心原则

- ✅ 不新增任何对话轮次或表单步骤
- ✅ 用户只需选择/简单描述金手指类型
- ✅ AI自动生成完整的金手指详细设定
- ✅ 两种模式统一输出到 `金手指设定.json`

---

## 一、两种模式的简化处理

### 1.1 对话打磨模式（保持6轮不变）

**第3轮（金手指选择）- 保持现有UI不变：**

```python
def _round_golden_finger(self, prev_choice: str, custom: str = None) -> Dict:
    """第三轮：金手指选择（保持现有逻辑）"""
    # 用户选择：side_effect_memory / unique_audience / ...
    self.creative_draft.golden_finger_type = prev_choice
    self.creative_draft.golden_finger = gf_map.get(prev_choice, prev_choice)
    if custom:
        self.creative_draft.golden_finger += f"（自定义：{custom}）"
    # ... 进入第4轮
```

**修改：最终方案生成时调用AI补全**

```python
def _round_generate_full_plan(self, prev_choice: str, custom: str = None) -> Dict:
    """第六轮：AI生成完整方案（增加金手指补全）"""
    
    # 1. 收集基础信息
    base_info = {
        "genre": self.genre,
        "protagonist": self.creative_draft.protagonist,
        "protagonist_type": self.creative_draft.protagonist_type,
        "golden_finger_type": self.creative_draft.golden_finger_type,
        "golden_finger_desc": self.creative_draft.golden_finger,
        "opening_design": self.creative_draft.opening_design,
        "emotion_line": self.creative_draft.emotion_line,
    }
    
    # 2. 🔥 AI生成完整金手指设定（自动补全）
    golden_finger_design = self._generate_golden_finger_with_ai(base_info)
    self.creative_draft.golden_finger_design = golden_finger_design
    
    # 3. 生成最终方案（包含完整金手指）
    full_plan = self._generate_plan_with_ai()  # 现有逻辑
    full_plan["golden_finger"] = golden_finger_design  # 追加金手指
    
    return full_plan

def _generate_golden_finger_with_ai(self, base_info: Dict) -> Dict:
    """
    基于用户选择的类型，AI自动生成完整的金手指详细设定
    """
    prompt = f"""基于以下信息，生成完整的金手指详细设定。

【题材】{base_info['genre']}
【主角性格】{base_info['protagonist']}（{base_info['protagonist_type']}）
【金手指类型】{base_info['golden_finger_type']}
【用户描述】{base_info['golden_finger_desc']}
【开局设计】{base_info['opening_design']}
【情感线】{base_info['emotion_line']}

请生成完整的金手指设定（JSON格式）：
{{
    "name": "金手指名称（有创意，符合题材）",
    "type": "{base_info['golden_finger_type']}",
    "concept": "核心概念（50字内，说清机制）",
    "initial_ability": "初始能力（刚获得时，限制较多）",
    "growth_curve": "成长曲线（前期/中期/后期，分阶段描述）",
    "max_potential": "最终形态（后期解锁的终极能力）",
    "limitations": ["限制1", "限制2", "限制3"],
    "side_effects": ["副作用1", "副作用2"],
    "cooldown_rules": "冷却规则",
    "combat_usage": "战斗应用场景",
    "daily_usage": "日常应用场景",
    "combo_with_protagonist": "与主角性格的联动效果",
    "plot_hooks": [
        "第3章关键剧情（首次使用金手指）",
        "第15章关键剧情（金手指进化/危机）",
        "第30章关键剧情（金手指的秘密揭晓）"
    ],
    "twist_potential": "潜在反转（金手指背后的真相）"
}}

要求：
1. 必须与主角性格【{base_info['protagonist_type']}】形成联动
2. 必须有明确的成长空间（前期弱→后期强）
3. 必须有代价或限制，不能无敌
4. 必须包含至少3个剧情钩子，对应1-30章
5. 书名是《{self.creative_draft.title or '待定'}》，金手指要与书名风格匹配"""

    # 调用AI生成
    response = self.api_client.generate_content_with_retry(
        content_type="golden_finger_design",
        user_prompt=prompt,
        temperature=0.8
    )
    
    # 解析JSON
    return parse_json_response(response)
```

### 1.2 表单模式（保持6步不变）

**第6步（详细设定）- 保持现有表单：**

```python
form_data = {
    "title": "书名",
    "protagonist_name": "主角名",
    "protagonist_personality": "主角性格",
    "golden_finger_desc": "用户输入的金手指描述（可选）",  # 简单描述即可
    # ... 其他字段
}
```

**修改：生成最终方案时调用AI补全**

```python
def generate_final_plan():
    # ... 现有代码 ...
    
    # 从 form_data 提取基础信息
    base_info = {
        "genre": genre,
        "title": form_data.get('title'),
        "protagonist_name": form_data.get('protagonist_name'),
        "protagonist_personality": form_data.get('protagonist_personality'),
        "golden_finger_desc": form_data.get('golden_finger_desc', ''),  # 可能为空
        "target_chapters": form_data.get('chapters', 200),
    }
    
    # 🔥 AI生成完整金手指设定（如果用户描述为空，AI根据题材自动生成）
    golden_finger_design = generate_golden_finger_with_ai(base_info)
    
    # 构建最终方案
    final_plan = {
        "title": base_info['title'],
        "protagonist_name": base_info['protagonist_name'],
        # ... 其他字段
        "golden_finger": golden_finger_design,  # 完整金手指结构
    }
    
    return final_plan
```

---

## 二、统一的数据结构

### 2.1 金手指设定.json 标准格式

```json
{
  "metadata": {
    "created_at": "2026-04-06T18:56:30",
    "created_by": "dialog_mode|form_mode",
    "version": "1.0"
  },
  "basic_info": {
    "name": "全民指令整活系统",
    "type": "unique_audience",
    "type_label": "📺 观众互动型",
    "concept": "全球观众可通过弹幕发起投票指令。完成的指令越离谱、对怪谈造成的心理阴影越大，获得的'整活点'越高，可兑换无视规则的逆天技能。"
  },
  "abilities": {
    "initial": "初始解锁'弹幕裁判权'：可从弹幕中任选一条指令强制对象执行（成功率与对象精神状态相关）。每日限10次。",
    "growth": "前期(1-30章)：每日增长1%规则扭曲度，每10点解锁新指令权。中期(31-80章)：可对自己发布指令实现'言出法随'。后期(81-100章)：整活点可具现化'概念级武器'。",
    "max": "终极形态'位面导演'：可改写局部规则，将怪谈世界临时转化为'整活直播间'。"
  },
  "restrictions": {
    "limitations": [
      "需要观众人数>1000才能激活高级功能",
      "每次使用消耗'整活点'，点数为0时系统休眠",
      "不能对自己使用负面指令"
    ],
    "side_effects": [
      "频繁使用会导致现实中的主角变得话痨",
      "长期依赖弹幕决策会削弱自主判断能力"
    ],
    "cooldown_rules": "普通指令冷却5分钟，高级指令冷却24小时"
  },
  "applications": {
    "combat": "利用弹幕指令让敌人执行尴尬动作（如强吻队友、跳钢管舞），破坏其战斗节奏，造成真实伤害。",
    "daily": "通过完成搞笑指令积累整活点，兑换生活便利技能（如瞬移、时间暂停等）。",
    "special": {
      "social_death_attack": "社死现场转化为实质攻击力，越尴尬伤害越高",
      "audience_summon": "召唤观众'云围观'增加心理压力"
    }
  },
  "protagonist_synergy": {
    "compatibility": "与话痨吐槽型主角完美契合",
    "combo_effects": [
      "话痨+整活：吐槽内容可直接转化为指令选项",
      "直播场景+弹幕系统：主场优势，效果翻倍",
      "毒舌+强制指令：心理打击双重暴击"
    ]
  },
  "plot_role": {
    "hooks": [
      {
        "chapter": 3,
        "title": "首次整活",
        "description": "主角收到'强吻裂口女'的弹幕指令，硬着头皮执行，没想到触发隐藏机制，获得巨量整活点"
      },
      {
        "chapter": 15,
        "title": "弹幕审判",
        "description": "全球观众投票决定主角是否该牺牲自己救队友，主角用骚操作化解两难困境"
      },
      {
        "chapter": 30,
        "title": "系统真相",
        "description": "发现系统其实是高位存在收集人类情绪的工具，主角开始怀疑'观众'的真实身份"
      }
    ],
    "twist_potential": "所谓的'全球观众'其实是历代失败者的灵魂，他们在等待一个能打破规则的人"
  }
}
```

---

## 三、核心代码改造

### 3.1 phase_one_generator.py 新增

```python
def generate_all_products(self, genre: str, tropes: Dict, plan: Dict) -> Dict:
    """生成所有第一阶段产物（增加金手指）"""
    
    products = {
        # ... 现有产物 ...
        
        # 新增：金手指详细设定
        "golden_finger_design": self._generate_golden_finger_product(plan),
    }
    
    return products

def _generate_golden_finger_product(self, plan: Dict) -> Dict:
    """生成金手指产物（从plan中提取或AI生成）"""
    
    # 如果 plan 中已有完整的 golden_finger，直接使用
    gf_data = plan.get("golden_finger", {})
    
    if gf_data and isinstance(gf_data, dict) and "abilities" in gf_data:
        # 已有完整结构，直接保存
        return gf_data
    
    # 否则，从简化信息生成
    return self._generate_golden_finger_with_ai(plan)

def _generate_golden_finger_with_ai(self, plan: Dict) -> Dict:
    """调用AI生成完整金手指设定"""
    
    # 收集上下文信息
    genre = plan.get("genre", "未知题材")
    protagonist = plan.get("protagonist", {})
    protagonist_name = protagonist.get("name", "主角")
    protagonist_personality = protagonist.get("personality", "冷静理智")
    
    # 获取金手指基础信息（可能是字符串或简单对象）
    gf_base = plan.get("golden_finger", plan.get("golden_finger_summary", ""))
    if isinstance(gf_base, str):
        gf_desc = gf_base
        gf_type = "custom"
    else:
        gf_desc = gf_base.get("concept", gf_base.get("name", ""))
        gf_type = gf_base.get("type", "custom")
    
    prompt = f"""请为以下小说生成完整的金手指详细设定。

【题材】{genre}
【主角】{protagonist_name}（{protagonist_personality}）
【金手指基础描述】{gf_desc}
【金手指类型】{gf_type}

请生成完整的金手指设定（必须是JSON格式）：
{{
    "name": "金手指名称（符合题材风格）",
    "type": "{gf_type}",
    "type_label": "类型标签",
    "concept": "核心概念描述",
    "abilities": {{
        "initial": "初始能力（限制较多）",
        "growth": "成长曲线（分阶段）",
        "max": "最终形态"
    }},
    "restrictions": {{
        "limitations": ["限制1", "限制2"],
        "side_effects": ["副作用1", "副作用2"],
        "cooldown": "冷却规则"
    }},
    "applications": {{
        "combat": "战斗应用",
        "daily": "日常应用",
        "special": {{}}
    }},
    "protagonist_synergy": {{
        "compatibility": "与主角契合度说明",
        "combo_effects": ["联动效果1", "联动效果2"]
    }},
    "plot_role": {{
        "hooks": [
            {{"chapter": 3, "title": "钩子标题", "description": "详细描述"}},
            {{"chapter": 15, "title": "钩子标题", "description": "详细描述"}},
            {{"chapter": 30, "title": "钩子标题", "description": "详细描述"}}
        ],
        "twist_potential": "潜在反转"
    }}}
}}

要求：
1. 金手指必须有成长性（前期弱→后期强）
2. 必须有明确的限制或代价
3. 必须与主角性格形成联动
4. 必须包含3个剧情钩子（对应第3、15、30章）
5. 必须符合【{genre}】题材特点"""

    try:
        response = self.api_client.generate_content_with_retry(
            content_type="golden_finger_design",
            user_prompt=prompt,
            temperature=0.8
        )
        
        # 解析JSON
        import json
        import re
        json_match = re.search(r'\{[\s\S]*\}', str(response))
        if json_match:
            return json.loads(json_match.group())
    except Exception as e:
        logger.error(f"AI生成金手指失败: {e}")
    
    # 回退：生成简化版本
    return self._create_fallback_golden_finger(gf_desc)

def _create_fallback_golden_finger(self, desc: str) -> Dict:
    """生成回退金手指设定"""
    return {
        "basic_info": {
            "name": "未命名系统",
            "type": "unknown",
            "concept": desc or "待补充"
        },
        "abilities": {
            "initial": "待补充",
            "growth": "待补充",
            "max": "待补充"
        },
        "restrictions": {
            "limitations": [],
            "side_effects": [],
            "cooldown": ""
        },
        "applications": {
            "combat": "",
            "daily": "",
            "special": {}
        },
        "protagonist_synergy": {
            "compatibility": "",
            "combo_effects": []
        },
        "plot_role": {
            "hooks": [],
            "twist_potential": ""
        },
        "_note": "AI生成失败，使用回退模板",
        "_needs_manual_completion": True
    }
```

### 3.2 phase_one_loader.py 改造

```python
def load_golden_finger(self) -> Dict:
    """加载金手指设定（统一入口）"""
    
    # 1. 优先从专用文件读取
    gf = self._load_json("金手指设定.json", None)
    if gf:
        logger.info(f"[PhaseOneDataLoader] 从金手指设定.json加载")
        return gf
    
    # 2. 从 project_info.json 读取
    project_info = self._load_project_info()
    if project_info:
        plan = project_info.get("plan", {})
        gf = plan.get("golden_finger", None)
        if gf and isinstance(gf, dict):
            # 检查是否是完整结构
            if "abilities" in gf or "basic_info" in gf:
                logger.info(f"[PhaseOneDataLoader] 从project_info.json加载完整金手指")
                return gf
            else:
                logger.warning(f"[PhaseOneDataLoader] 金手指结构不完整")
        
        # 3. 尝试读取旧字段
        summary = plan.get("golden_finger_summary", "")
        if summary:
            logger.warning(f"[PhaseOneDataLoader] 使用旧版golden_finger_summary回退")
            return self._create_simple_golden_finger(summary)
    
    # 4. 从世界观回退
    world = self.load_world_setting()
    power = world.get("power_system", {})
    mechanics = power.get("mechanics", {})
    mc_exclusive = mechanics.get("mc_exclusive", "")
    
    if mc_exclusive:
        logger.warning(f"[PhaseOneDataLoader] 从世界观mc_exclusive回退")
        return self._create_simple_golden_finger(mc_exclusive)
    
    # 5. 空对象（不再硬编码）
    logger.error(f"[PhaseOneDataLoader] 无法找到金手指设定")
    return {
        "basic_info": {"name": "未设定", "concept": ""},
        "_error": "金手指设定缺失"
    }

def _create_simple_golden_finger(self, desc: str) -> Dict:
    """从简单描述创建简化结构"""
    return {
        "basic_info": {
            "name": "待命名系统",
            "type": "unknown",
            "concept": desc
        },
        "abilities": {
            "initial": desc,
            "growth": "待补充",
            "max": "待补充"
        },
        "restrictions": {"limitations": [], "side_effects": [], "cooldown": ""},
        "applications": {"combat": "", "daily": "", "special": {}},
        "protagonist_synergy": {"compatibility": "", "combo_effects": []},
        "plot_role": {"hooks": [], "twist_potential": ""},
        "_source": "fallback",
        "_needs_completion": True
    }
```

---

## 四、实施步骤

### 阶段1：数据层改造（优先级：高）

1. **修改 `phase_one_generator.py`**
   - 新增 `_generate_golden_finger_product()` 方法
   - 在 `generate_all_products()` 中添加金手指产物生成
   - AI自动从基础信息生成完整设定

2. **修改 `phase_one_loader.py`**
   - 改造 `load_golden_finger()` 统一入口
   - 优先读取 `金手指设定.json`
   - 移除硬编码的"弹幕干涉系统"

### 阶段2：对话打磨模式适配（优先级：高）

1. **修改 `_round_generate_full_plan()`**
   - 在生成最终方案前，调用AI生成完整金手指
   - 将完整金手指结构存入 `creative_draft`

2. **修改 `_generate_plan_with_ai()`**
   - 确保生成的 `final_plan` 包含完整的 `golden_finger` 字段

### 阶段3：表单模式适配（优先级：中）

1. **修改 `generate_final_plan()` API**
   - 在生成最终方案时，调用AI补全金手指
   - 即使用户只输入简单描述，也能生成完整结构

---

## 五、验证要点

### 对话打磨模式
- [ ] 用户选择金手指类型（第3轮）
- [ ] 第6轮AI自动生成完整金手指结构
- [ ] `金手指设定.json` 包含完整字段
- [ ] 章节生成时使用AI生成的金手指

### 表单模式
- [ ] 用户可输入简单金手指描述
- [ ] AI自动补全为完整结构
- [ ] 生成的 `金手指设定.json` 与对话模式一致

### 数据兼容性
- [ ] 旧项目（无金手指文件）使用回退逻辑
- [ ] 新项目自动生成完整金手指
- [ ] 不再出现硬编码"弹幕干涉系统"

---

## 六、关键优势

| 改进点 | 效果 |
|--------|------|
| 不新增步骤 | 用户体验不变，流程顺畅 |
| AI自动补全 | 降低用户输入负担，保证设定完整性 |
| 统一数据结构 | 两种模式输出一致，便于维护 |
| 专用产物文件 | 金手指独立存储，不再散落在各处 |
| 移除硬编码 | 章节生成真正使用用户设定的金手指 |

---

*方案版本：v2.0（简化版）*
*核心变更：AI自动生成详细设定，不增加用户操作步骤*
