# 角色一致性保障方案分析报告

## 问题诊断

### 现象
- 章节生成过程中角色设定偏离最初设计
- 出现许多未经设计的新增角色
- 战术规划未充分引入角色信息

### 根因分析

1. **战术规划缺乏角色设定**
   - `TacticalPlanner` 主要关注情绪曲线和事件节点
   - 仅传入 `protagonist_name`（主角名），缺乏详细设定
   - 未包含：性格、能力、金手指、世界观等

2. **章节生成提示词不完整**
   - `_build_protagonist_reminder()` 仅提醒主角名
   - 缺乏金手指、卖点、世界观的强调
   - 缺乏配角设定的系统性传递

3. **批次间设定衰减**
   - 每批 6 章生成间隔，AI 对设定的记忆衰减
   - 缺乏每批开始前的"设定回顾"环节

---

## 解决方案

### 方案一：增强战术规划的角色信息（推荐）

**修改点**: `TacticalPlanner._build_tactical_prompt()`

**方案**: 在战术规划提示词中增加详细的角色设定板块

```python
# 在 _build_tactical_prompt 中增加：
character_info = f"""
### 核心设定（必须严格遵守）

**主角**: {protagonist_name}
- 性格: {novel_data.get('protagonist_personality', '未设定')}
- 能力/金手指: {novel_data.get('golden_finger_summary', '未设定')}
- 初始身份: {novel_data.get('protagonist_background', '未设定')}
- 核心卖点: {novel_data.get('core_selling_point', '未设定')}

**世界观**: {novel_data.get('world_setting', '未设定')}

**已登场配角**:
{self._format_existing_characters(novel_data)}

**禁忌**: 严禁随意创造新角色，所有角色必须在上述设定中已存在
"""
```

---

### 方案二：章节生成增加设定回顾（每批第一章）

**修改点**: `ChapterConversationGenerator._generate_single_chapter_in_session()`

**方案**: 检测是否为批次第一章，增加设定回顾 prompt

```python
def _generate_single_chapter_in_session(self, chapter_num: int, blueprint: Dict, prev_summary: str) -> Dict:
    # 检测是否为批次第一章
    is_batch_first = (chapter_num % 6 == 1) or (chapter_num == 1)
    
    if is_batch_first:
        setup_review = self._build_batch_setup_review()
        # 将设定回顾插入对话上下文
        self._insert_setup_review_to_context(setup_review)
    
    # 继续正常生成流程...

def _build_batch_setup_review(self) -> str:
    """构建批次设定回顾"""
    return f"""
🚨 **批次设定回顾**（第{self.current_batch}批，第{self.current_chapter}-{self.current_chapter+5}章）

**核心设定 - 绝对不可偏离**:
1. 主角: {self.novel_data.get('protagonist_name', '未设定')} - {self.novel_data.get('protagonist_personality', '')}
2. 金手指: {self.novel_data.get('golden_finger_summary', '')}
3. 核心卖点: {self.novel_data.get('core_selling_point', '')}
4. 世界观: {self.novel_data.get('story_direction', '')}
5. 已登场角色: {self._get_existing_characters()}

**禁忌**:
- ❌ 禁止创造未经设计的新角色
- ❌ 禁止改变主角核心性格
- ❌ 禁止修改金手指规则

**本批重点**: {self._get_batch_focus()}
"""
```

---

### 方案三：增强 System Prompt 的角色约束

**修改点**: `ChapterConversationGenerator._build_system_prompt()`

**方案**: 在 System Prompt 中增加强制角色一致性约束

```python
def _build_system_prompt(self) -> str:
    base_prompt = self._build_base_system_prompt()
    
    # 增加角色一致性约束
    character_constraint = f"""

## 角色一致性约束（严格必须遵守）

**主角设定**:
- 姓名: {self.novel_data.get('protagonist_name', '主角')}
- 性格: {self.novel_data.get('protagonist_personality', '')}
- 能力: {self.novel_data.get('golden_finger_summary', '')}
- 行为模式: {self._get_protagonist_behavior_pattern()}

**配角限制**:
- 仅允许使用已登场角色: {self._get_existing_characters()}
- 禁止新增未经批准的角色
- 配角对话不得超过 200 字

**违规检测**:
每章完成后需检查：
1. 是否出现新角色？
2. 主角行为是否符合性格设定？
3. 金手指使用是否一致？
"""
    
    return base_prompt + character_constraint
```

---

### 方案四：增加质检规则（策略性）

**修改点**: `ChapterQualityChecker`

**方案**: 在章节质检中增加角色一致性检查

```python
# 在质检配置中增加：
CHARACTER_CHECK_RULES = {
    "new_character_check": {
        "description": "检查是否有未经设计的新角色",
        "severity": "high",
        "check_method": "compare_with_design"
    },
    "protagonist_behavior_check": {
        "description": "检查主角行为是否符合性格设定",
        "severity": "high",
        "check_method": "personality_consistency"
    },
    "golden_finger_check": {
        "description": "检查金手指使用是否一致",
        "severity": "medium",
        "check_method": "ability_consistency"
    }
}
```

---

## 推荐实施顺序

### 第一阶段（高优先级）

1. **方案一 + 方案三**
   - 修改战术规划，增加角色设定板块
   - 修改 System Prompt，增加角色约束
   - 影响：所有章节都会受益

### 第二阶段（中优先级）

2. **方案二**
   - 在每批第一章增加设定回顾
   - 特别适用于长篇小说（超过24章后设定衰减严重）

### 第三阶段（低优先级）

3. **方案四**
   - 增加质检规则，自动检测角色一致性问题
   - 需要额外的 AI 调用，成本较高

---

## 预期效果

| 指标 | 改进前 | 改进后 |
|------|--------|--------|
| 角色一致性 | 60% | 90%+ |
| 新增无故障角色 | 高（每627章） | 低（<1个/批） |
| 设定偏离 | 频繁 | 极少 |
| 用户干预需求 | 高 | 低 |

---

## 待确认问题

1. **设定回顾频率**：
   - 每批第一章（每6章）？
   - 还是每个阶段第一章（每30章）？
   - 或者用户主动触发？

2. **角色设定来源**：
   - 优先使用 `user_choices` （用户填写）？
   - 还是 `final_plan` （AI生成的方案）？
   - 或者两者合并？

3. **配角管理**：
   - 是否允许AI在特定情况下创造新配角（如反派手下）？
   - 还是严格限制在已设计角色列表中？

---

## 下一步行动

建议按以下顺序实施：

1. **确认上述待确认问题**
2. **实施方案一（战术规划增强）**
3. **实施方案三（System Prompt增强）**
4. **测试验证效果**
5. **根据反馈决定是否需要方案二和四**
