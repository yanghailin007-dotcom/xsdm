# 基于现有系统的修复方案

## 现有流程回顾

```
用户输入 → 对话打磨 → final_plan → novel_data → 战术规划 → 章节生成
                                            → blueprint
```

**既有数据流**:
- `novel_data`: 包含 final_plan, user_choices, world_state
- `blueprint`: 战术规划，但缺乏角色设定细节
- 章节生成: 使用 `novel_data` 和 `blueprint`，但提示词不完整

---

## 问题1: 核心设定丢失 — 修复方案

### 修复点1: 增强章节生成的设定提醒

**修改文件**: `web/services/market_driven/chapter_conversation_generator.py`

**修改位置**: `_build_full_prompt()` 方法

**具体修改**:
```python
def _build_core_setting_reminder(self) -> str:
    """构建核心设定提醒（基于现有 novel_data）"""
    
    # 从 novel_data 获取设定（使用现有字段）
    final_plan = self.novel_data.get('final_plan', {})
    user_choices = self.novel_data.get('user_choices', {})
    
    return f"""
🚨 **核心设定强制约束**（绝对不可偏离）

**主角**
- 姓名: {user_choices.get('protagonist_name', final_plan.get('protagonist_name', '主角'))}
- 性格: {final_plan.get('protagonist_personality', user_choices.get('protagonist_personality', '未设定'))}
- 能力/金手指: {final_plan.get('golden_finger_summary', user_choices.get('golden_finger_desc', '未设定'))}
- 初始身份: {final_plan.get('protagonist_background', '未设定')}

**核心卖点**  
{final_plan.get('core_selling_point', user_choices.get('sellpoint', '未设定'))}

**世界观**
{final_plan.get('story_direction', '未设定')}

**禁忌**
- 禁止修改主角性格
- 禁止修改金手指规则  
- 禁止偏离核心卖点
"""
```

**使用方式**:
```python
def _build_full_prompt(self, ...):
    # 现有逻辑...
    
    # 新增：每章都添加核心设定提醒
    core_reminder = self._build_core_setting_reminder()
    
    full_prompt = core_reminder + "\n\n" + protagonist_reminder + world_state_constraint + chapter_prompt
    
    return full_prompt
```

---

### 修复点2: 增强战术规划的设定传递

**修改文件**: `web/services/market_driven/tactical_planner.py`

**修改位置**: `_build_tactical_prompt()` 方法

**具体修改**:
```python
def _build_tactical_prompt(self, ..., novel_data: Dict = None, ...):
    """构建战术规划提示词"""
    
    # 从 novel_data 获取核心设定
    final_plan = novel_data.get('final_plan', {}) if novel_data else {}
    user_choices = novel_data.get('user_choices', {}) if novel_data else {}
    
    character_info = f"""
### 核心设定（战术规划必须遵守）

**主角设定**
- 姓名: {user_choices.get('protagonist_name', final_plan.get('protagonist_name', '主角'))}
- 性格: {final_plan.get('protagonist_personality', '未设定')}
- 能力: {final_plan.get('golden_finger_summary', '未设定')}

**核心卖点**
{final_plan.get('core_selling_point', '未设定')}

**战术要求**
- 每章必须体现核心卖点
- 主角行为必须符合性格设定
- 金手指使用必须一致
"""
    
    # 将 character_info 插入到提示词中
    ...
```

---

### 修复点3: 批次第一章增加设定回顾

**修改文件**: `web/services/market_driven/chapter_conversation_generator.py`

**修改位置**: `_generate_single_chapter_in_session()` 方法

**具体修改**:
```python
def _generate_single_chapter_in_session(self, chapter_num: int, blueprint: Dict, prev_summary: str) -> Dict:
    # 检测是否为批次第一章（6章一批）
    is_batch_first = (chapter_num % 6 == 1)
    
    if is_batch_first:
        # 新增：批次设定回顾
        batch_review = self._build_batch_setting_review(chapter_num)
        
        # 插入到对话上下文中
        self._insert_to_conversation_context(batch_review)
    
    # 继续原有逻辑...

def _build_batch_setting_review(self, chapter_num: int) -> str:
    """构建批次设定回顾"""
    final_plan = self.novel_data.get('final_plan', {})
    user_choices = self.novel_data.get('user_choices', {})
    
    return f"""
📋 **批次设定回顾** (第{chapter_num}-{chapter_num+5}章)

**必须严格遵守的核心设定**
1. **主角**: {user_choices.get('protagonist_name', final_plan.get('protagonist_name', '主角'))}
   - 性格: {final_plan.get('protagonist_personality', '')}
   - 能力: {final_plan.get('golden_finger_summary', '')}

2. **核心卖点**: {final_plan.get('core_selling_point', '')}

3. **世界观**: {final_plan.get('story_direction', '')}

**存在偏离将导致设定崩坏，请严格遵守**
"""
```

---

## 问题2: AI乱创造角色 — 修复方案

### 修复点1: 基于现有 world_state 增强角色约束

**修改文件**: `web/services/market_driven/chapter_conversation_generator.py`

**修改位置**: `_build_world_state_constraint()` 方法（新增）

**具体修改**:
```python
def _build_character_constraint(self) -> str:
    """基于 world_state 构建角色约束"""
    
    # 使用现有 world_state 管理器获取角色信息
    if not self.world_state_manager:
        return ""
    
    world_state = self.world_state_manager.get_full_state()
    
    # 获取已登场角色
    protagonist = world_state.get('protagonist', {})
    allies = world_state.get('allies', {})
    enemies = world_state.get('enemies', {})
    
    # 构建角色列表
    character_list = []
    
    # 主角
    if protagonist:
        character_list.append(f"主角: {protagonist.get('name', '未命名')} - {protagonist.get('current_state', '')}")
    
    # 配角（只列出重要配角，限制5个）
    ally_list = list(allies.items())[:5]
    for name, info in ally_list:
        character_list.append(f"配角: {name} - {info.get('relationship', '')}")
    
    # 敌人（只列出重要敌人，限制3个）
    enemy_list = list(enemies.items())[:3]
    for name, info in enemy_list:
        character_list.append(f"敌人: {name} - {info.get('role', '')}")
    
    return f"""
### 角色约束（基于已登场角色）

**已登场角色列表**（仅限使用这些角色）:
{chr(10).join(['- ' + c for c in character_list])}

**角色使用规则**
1. **主角**: 每章必须出场
2. **配角**: 根据场景选择，但必须在上述列表中
3. **敌人**: 根据剧情需要出场
4. **临时角色**: 路人/小兵可以出场，但不得有姓名和详细设定

**禁忌**
- ❌ 禁止创造列表之外的新角色
- ❌ 禁止给临时角色起名字
- ❌ 禁止让已下线角色突然出场
"""
```

---

### 修复点2: 增加角色出场控制

**修改文件**: `web/services/market_driven/chapter_conversation_generator.py`

**修改位置**: `_generate_single_chapter_in_session()` 方法

**具体修改**:
```python
def _generate_single_chapter_in_session(self, chapter_num: int, blueprint: Dict, prev_summary: str) -> Dict:
    # 获取本章规划
    chapter_plan = self._get_chapter_plan(chapter_num, blueprint)
    
    # 新增：获取推荐出场角色
    recommended_chars = self._get_recommended_characters(chapter_num, chapter_plan)
    
    # 构建提示词时传入推荐角色
    prompt = self._build_full_prompt(
        chapter_num=chapter_num,
        chapter_plan=chapter_plan,
        prev_summary=prev_summary,
        recommended_characters=recommended_chars  # 新增参数
    )
    
    # 继续生成...

def _get_recommended_characters(self, chapter_num: int, chapter_plan: Dict) -> List[str]:
    """根据章节规划和 world_state，推荐应该出场的角色"""
    
    if not self.world_state_manager:
        return []
    
    world_state = self.world_state_manager.get_full_state()
    
    recommended = []
    
    # 主角必须出场
    protagonist = world_state.get('protagonist', {})
    if protagonist:
        recommended.append(protagonist.get('name', '主角'))
    
    # 根据章节事件选择配角
    event = chapter_plan.get('event', '')
    allies = world_state.get('allies', {})
    
    # 简单关键词匹配（可以扩展）
    if '战斗' in event or '打' in event:
        # 战斗场景推荐战友
        for name in list(allies.keys())[:2]:
            recommended.append(name)
    elif '对话' in event or '交谈' in event:
        # 对话场景推荐关系好的配角
        for name, info in list(allies.items())[:1]:
            if '好' in info.get('relationship', ''):
                recommended.append(name)
    
    return recommended
```

---

### 修复点3: 简化版角色审查（可选）

**修改文件**: `web/services/market_driven/chapter_conversation_generator.py`

**修改位置**: 生成完成后检查

**具体修改**:
```python
def _generate_single_chapter_in_session(self, ...):
    # 生成章节...
    chapter = self._call_api(...)
    
    # 新增：简单版角色审查
    if self.world_state_manager:
        check_result = self._simple_character_check(chapter.get('content', ''))
        if check_result.get('has_new_character'):
            logger.warning(f"第{chapter_num}章可能有新角色: {check_result.get('new_chars')}")
            # 可选：添加警告标记
            chapter['character_warning'] = check_result.get('new_chars')
    
    return chapter

def _simple_character_check(self, content: str) -> Dict:
    """简单版角色检查（基于已有角色名单）"""
    
    # 获取已有角色名单
    existing_names = set()
    world_state = self.world_state_manager.get_full_state()
    
    protagonist = world_state.get('protagonist', {})
    if protagonist:
        existing_names.add(protagonist.get('name', ''))
    
    existing_names.update(world_state.get('allies', {}).keys())
    existing_names.update(world_state.get('enemies', {}).keys())
    
    # 简单检查：查找可能的人名（带姓氏的词）
    import re
    potential_names = re.findall(r'[一-龥]{2,4}(?=先生|小姐|少侠|大人|阿[佬姨])', content)
    
    new_chars = [name for name in potential_names if name not in existing_names]
    
    return {
        'has_new_character': len(new_chars) > 0,
        'new_chars': new_chars,
        'existing_count': len(existing_names)
    }
```

---

## 实施计划

### 第一阶段（第1周）
1. 实施修复点1：增强章节生成的核心设定提醒
2. 实施修复点2：增强战术规划的设定传递

### 第二阶段（第2周）
3. 实施修复点3：批次第一章增加设定回顾
4. 实施修复点4：基于 world_state 增强角色约束

### 第三阶段（第3周）
5. 实施修复点5：增加角色出场控制
6. 实施修复点6（可选）：简化版角色审查

---

## 预期效果

| 问题 | 改进前 | 改进后 |
|------|--------|--------|
| 核心设定丢失 | 频繁 | 每章都有强制提醒 |
| 角色出场无约束 | 随意 | 基于列表限制 |
| 新角色氪漫 | 每5-7章一个 | 筛选后每10-15章一个 |
| 存活角色被遗弃 | 常见 | 按规划多次出场 |

---

## 技术风险

1. **提示词过长**：每章都加设定提醒可能导致token超限
   - 解决：第一段提供简化版设定

2. **角色约束过严**：可能限制剧情发展
   - 解决：允许临时角色（路人小兵），只限制有名角色

3. **检查准确率**：简单关键词匹配可能误报
   - 解决：仅作为警告，不阻止生成
