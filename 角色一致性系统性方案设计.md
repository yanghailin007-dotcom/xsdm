# 角色一致性系统性方案设计

## 现有问题分析

### 问题 1: 角色信息在各阶段传递断层

```
用户输入 → 对话打磨 → 战略规划 → 战术规划 → 章节生成
                                              ↓
                                         ❌ 信息丢失
```

**具体表现**:
- 对话打磨收集了详细设定（如"主角+二哈毒舌组合"）
- 战略规划只保留了基础框架
- 战术规划只有主角名
- 章节生成时基本没有角色设定

### 问题 2: 新角色无限制创造

AI为了推进剧情，不断创造新角色，导致：
- 角色异常增多（每7章一个新角色）
- 原有配角被遗弃
- 主角特色被冲淡

---

## 新方案设计: 分层级角色管理系统

### 核心思想

**不是禁止新角色，而是管理新角色**

```
角色分类:
- 核心角色（主角+标配）: 必须严格遵守设定，不得偏离
- 重要配角（反派/盟友）: 允许AI创造，但需要审批
- 临时角色（路人/小兵）: 完全开放，随用随走
```

---

## 方案架构

### 第一层: 角色设计中心（Character Design Center）

**定位**: 集中管理所有角色设定

**数据结构**:
```python
@dataclass
class CharacterDesign:
    # 基础信息
    name: str
    role_type: str  # protagonist | sidekick | ally | villain | neutral
    importance: str  # core | major | minor | temporary
    
    # 核心设定（严格约束）
    personality: str  # 性格特征
    speech_pattern: str  # 说话方式（如"二哈毒舌风格"）
    abilities: List[str]  # 能力/金手指
    
    # 变化轨迹（可调整）
    current_state: str  # 当前状态
    arc_plan: str  # 成长轨迹
    
    # 关系网
    relationships: Dict[str, str]  # 角色关系
    
    # 出场管理
    first_appearance: int  # 首次出场章节
    appearance_frequency: str  # 出场频率: high | medium | low
```

**存储位置**: `project_data/character_design.json`

---

### 第二层: 角色钩子监控（Character Hook Monitor）

**定位**: 跟踪角色钩子的埋下和回收

**功能**:
```python
class CharacterHookMonitor:
    def __init__(self, characters: List[CharacterDesign]):
        self.hooks = []
    
    def plant_hook(self, character: str, hook_type: str, content: str, target_chapter: int):
        """埋下角色钩子"""
        pass
    
    def check_hook_recovery(self, chapter_num: int) -> List[Dict]:
        """检查应该在本章回收的钩子"""
        pass
    
    def verify_hook_consistency(self, chapter_content: str) -> bool:
        """验证章节内容是否回收了钩子"""
        pass
```

**使用场景**:
- 第1章埋下"主角身上有神秘印记"
- 第15章提醒AI"必须回收印记钩子"
- 自动检查章节内容是否完成回收

---

### 第三层: 智能角色引入（Smart Character Introduction）（重点）

**定位**: 解决"新角色无限制"问题

**核心机制**:

#### 1. 角色库管理
```python
class CharacterPool:
    """分层级角色库
    
    Level 1: 必须出场（核心角色）
    Level 2: 建议出场（重要配角）
    Level 3: 可选出场（临时角色）
    """
    
    def get_characters_for_chapter(self, chapter_num: int, scene_type: str) -> List[CharacterDesign]:
        """根据章节和场景，推荐应该出场的角色
        
        Args:
            chapter_num: 章节号
            scene_type: 场景类型（战斗/对话/探索）
        """
        # 例如：战斗场景必须有主角+战友
        # 对话场景可以有主角+任意配角
        pass
```

#### 2. 新角色审批流程
```python
class NewCharacterApproval:
    """新角色引入审批
    
    不是禁止，而是审批！
    """
    
    def evaluate_new_character(self, proposed_character: Dict, context: Dict) -> Dict:
        """
        评估是否允许引入新角色
        
        Returns:
            {
                "approved": bool,
                "reason": str,
                "suggestions": List[str],
                "alternative": str  # 建议使用现有角色替代
            }
        """
        # 评估维度:
        # 1. 是否必要（能否用现有角色完成）
        # 2. 是否符合世界观
        # 3. 是否有后续用途（不是一次性角色）
        # 4. 角色数量是否超标
        pass
```

#### 3. 角色出场推荐系统
在每章生成前，自动推荐应该出场的角色：

```python
# 示例：第47章战斗场景
recommended_characters = character_pool.recommend_for_scene(
    chapter_num=47,
    scene_type="battle",
    required_roles=["protagonist"],  # 必须有主角
    optional_roles=["sidekick", "ally"],  # 建议有配角
    avoid_characters=["villain_boss"],  # 本章不出现反派BOSS
    max_characters=4  # 最多4个角色
)

# 返回:
# {
#   "must_include": ["主角", "二哈"],
#   "should_include": ["战友A"],
#   "can_include": ["路人甲"],
#   "forbidden": ["反派BOSS"]
# }
```

---

### 第四层: 动态角色提醒（Dynamic Character Reminder）

**定位**: 根据当前章节动态生成角色提醒

**实现**:
```python
class DynamicCharacterReminder:
    def build_reminder(self, chapter_num: int, scene_context: Dict) -> str:
        """根据章节和场景，动态构建角色提醒"""
        
        # 获取应该出场的角色
        characters = self.get_relevant_characters(chapter_num, scene_context)
        
        reminder_parts = []
        
        # 核心角色（必须出场）
        for char in characters["core"]:
            reminder_parts.append(f"""
### {char.name} [核心角色]
- 性格: {char.personality}
- 说话方式: {char.speech_pattern}
- 当前状态: {char.current_state}
- 本章任务: {char.get_chapter_mission(chapter_num)}
""")
        
        # 重要配角（建议出场）
        if characters["major"]:
            reminder_parts.append("\n### 建议出场配角")
            for char in characters["major"]:
                reminder_parts.append(f"- {char.name}: {char.get_appearance_reason(chapter_num)}")
        
        # 角色钩子回收提醒
        hooks = self.hook_monitor.get_pending_hooks_for_chapter(chapter_num)
        if hooks:
            reminder_parts.append("\n### 本章必须回收的钩子")
            for hook in hooks:
                reminder_parts.append(f"- [{hook.character}] {hook.content}")
        
        return "\n".join(reminder_parts)
```

---

## 实施方案

### 阶段一: 基础设施（第1-2周）

**1. Character Design Center**
- 创建 `character_design.json` 结构
- 从 `user_choices` 和 `final_plan` 导入角色设定
- 建立角色分类（核心/重要/临时）

**2. Character Hook Monitor**
- 实现钩子埋下/回收提醒
- 与现有的 `world_state.json` 整合

### 阶段二: 智能管理（第3-4周）

**3. Character Pool**
- 实现角色出场推荐算法
- 根据场景类型推荐角色组合

**4. New Character Approval**
- 实现新角色评估流程
- 提供"使用现有角色"的替代建议

### 阶段三: 动态优化（第5-6周）

**5. Dynamic Character Reminder**
- 在每章生成前自动生成角色提醒
- 根据章节进度更新角色状态

---

## 使用示例

### 场景1: 第1章开始
```
动态提醒:
### 林凡 [核心角色]
- 性格: 活瘘话瘘、毒舌、表面佛系
- 说话方式: 带着二哈一起吐槽，常用"哼"、"切"等语气词
- 当前状态: 被选中进入禁地，心不甘情不愿
- 本章任务: 建立人设，展示金手指"酒剑仙"能力

### 二哈 [核心配角]
- 特征: 能看见直播弹幕，会咬字回复
- 说话方式: "旺旺！"、"主人大傻逼"
- 本章任务: 与主角配合吐槽外国选手

禁止: 出现其他固定配角
```

### 场景2: 第15章（AI想创造新反派）
```
AI尝试: "让一个叫'盗甲'的神秘人出场"

审批系统:
- 评估: 这个角色是否必要？
- 结论: 可以用现有反派"黑狐"完成同样功能
- 建议: "使用已有反派'黑狐'，符合'神秘导师'设定"
- AI接受建议，使用现有角色
```

### 场景3: 第47章（战斗场景）
```
推荐角色:
必须出场: 主角、二哈
建议出场: 战友A（之前签约的团队成员）
可选出场: 敌方小兵（临时角色）
禁止出场: 反派BOSS（应该在第50章才出场）

钩子回收提醒: 本章必须回收"主角印记发热"的钩子
```

---

## 预期效果

| 问题 | 改进前 | 改进后 |
|------|--------|--------|
| 无故障新角色 | 每5-7章一个 | 筛选后每15-20章一个 |
| 配角边缘化 | 出场后很快消失 | 按计划多次出场 |
| 角色设定一致性 | 60% | 90%+ |
| 用户干预需求 | 每5章修改 | 每20章确认 |

---

## 待确认问题

1. **角色审批方式**:
   - AI自动审批（低成本，可能出错）？
   - 等待用户确认（高成本，准确）？
   - 混合模式（AI初审+用户复审重要角色）？

2. **角色设计来源**:
   - AI生成后用户确认？
   - 用户主导AI辅助？
   - AI初稿+用户修改？

3. **特殊角色处理**:
   - 一次性角色（路人甲、小兵乙）是否需要审批？
   - 敌方阶梯（小BOSS、中BOSS、大BOSS）如何分级管理？
